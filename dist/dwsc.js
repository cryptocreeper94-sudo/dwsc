// Lume Compiled Bundle — Browser
// Zero Dependencies — Built with Lume
// Source: src/main.lume
// Generated by Lume Compiler v0.8.0

(function() {
"use strict";

// ═══ Lume Standard Library (Browser) ═══
const text = {
  upper: (s) => String(s).toUpperCase(),
  lower: (s) => String(s).toLowerCase(),
  trim: (s) => String(s).trim(),
  split: (s, sep) => String(s).split(sep),
  join: (arr, sep = ', ') => arr.join(sep),
  replace: (s, from, to) => String(s).replaceAll(from, to),
  contains: (s, sub) => String(s).includes(sub),
  length: (s) => String(s).length,
};

const math = {
  abs: Math.abs, ceil: Math.ceil, floor: Math.floor, round: Math.round,
  min: (...a) => Math.min(...a), max: (...a) => Math.max(...a),
  random: () => Math.random(),
  random_int: (min, max) => Math.floor(Math.random() * (max - min + 1)) + min,
  clamp: (v, lo, hi) => Math.min(Math.max(v, lo), hi),
};

const list = {
  first: (a) => a[0], last: (a) => a[a.length - 1],
  map: (a, fn) => a.map(fn), filter: (a, fn) => a.filter(fn),
  range: (start, end) => { const r = []; for (let i = start; i < end; i++) r.push(i); return r; },
  count: (a) => a.length,
};

const dom = {
  create: (tag, opts = {}) => {
    const el = document.createElement(tag);
    if (opts.text) el.textContent = opts.text;
    if (opts.html) el.innerHTML = opts.html;
    if (opts.id) el.id = opts.id;
    if (opts.className) el.className = opts.className;
    if (opts.styles) Object.assign(el.style, opts.styles);
    if (opts.attrs) { for (const [k, v] of Object.entries(opts.attrs)) el.setAttribute(k, v); }
    if (opts.children) { for (const c of opts.children) el.appendChild(typeof c === 'string' ? document.createTextNode(c) : c); }
    if (opts.onClick) el.addEventListener('click', opts.onClick);
    return el;
  },
  select: (s) => document.querySelector(s),
  select_all: (s) => [...document.querySelectorAll(s)],
  add_child: (p, c) => { if (typeof p === 'string') p = document.querySelector(p); if (typeof c === 'string') c = document.createTextNode(c); p.appendChild(c); return c; },
  set_text: (el, t) => { if (typeof el === 'string') el = document.querySelector(el); el.textContent = t; },
  set_html: (el, h) => { if (typeof el === 'string') el = document.querySelector(el); el.innerHTML = h; },
  set_style: (el, p, v) => { if (typeof el === 'string') el = document.querySelector(el); el.style[p] = v; },
  set_styles: (el, s) => { if (typeof el === 'string') el = document.querySelector(el); Object.assign(el.style, s); },
  add_class: (el, ...c) => { if (typeof el === 'string') el = document.querySelector(el); el.classList.add(...c); },
  remove_class: (el, ...c) => { if (typeof el === 'string') el = document.querySelector(el); el.classList.remove(...c); },
  toggle_class: (el, c) => { if (typeof el === 'string') el = document.querySelector(el); el.classList.toggle(c); },
  on: (el, ev, fn) => { if (typeof el === 'string') el = document.querySelector(el); el.addEventListener(ev, fn); },
  mount: (el, t) => { const p = t ? (typeof t === 'string' ? document.querySelector(t) : t) : document.body; p.appendChild(el); return el; },
  inject_css: (css, id) => {
    if (id) { const e = document.getElementById(id); if (e) { e.textContent = css; return e; } }
    const s = document.createElement('style'); if (id) s.id = id; s.textContent = css; document.head.appendChild(s); return s;
  },
  animate: (el, kf, opts = {}) => {
    if (typeof el === 'string') el = document.querySelector(el);
    return el.animate(kf, { duration: opts.duration || 1000, easing: opts.easing || 'ease', iterations: opts.iterations || 1, fill: opts.fill || 'forwards', delay: opts.delay || 0 });
  },
  remove: (el) => { if (typeof el === 'string') el = document.querySelector(el); if (el && el.parentNode) el.parentNode.removeChild(el); },
  clear: (el) => { if (typeof el === 'string') el = document.querySelector(el); while (el.firstChild) el.removeChild(el.firstChild); },
  ready: (fn) => { if (document.readyState !== 'loading') fn(); else document.addEventListener('DOMContentLoaded', fn); },
};

const state = {
  machine: (cfg) => {
    let cur = cfg.initial; const ls = [];
    return {
      get current() { return cur; },
      send(ev) { const sc = cfg.states[cur]; if (sc && sc.on && sc.on[ev]) { const nx = sc.on[ev]; const pv = cur; cur = typeof nx === 'string' ? nx : nx.target; if (typeof nx === 'object' && nx.action) nx.action(pv, cur); ls.forEach(fn => fn(cur, pv, ev)); } return cur; },
      on_change(fn) { ls.push(fn); },
    };
  },
  reactive: (init) => {
    let v = init; const ls = [];
    return {
      get: () => v,
      set: (nv) => { const o = v; v = nv; ls.forEach(fn => fn(v, o)); },
      on_change: (fn) => { ls.push(fn); },
      bind: (el) => { if (typeof el === 'string') el = document.querySelector(el); el.textContent = v; ls.push((nv) => { el.textContent = nv; }); },
    };
  },
};

// ═══ Application Code ═══

// ═══════════════════════════════════════════════════════════
//  DWSC.io — DarkWave Systems Collective
//  Built with Lume — the AI-native programming language
//  
//  This is the Lume source file. It compiles to browser-ready
//  JavaScript via: lume bundle src/main.lume --target=browser
//
//  Ultra-Premium Protocol: Void Black + Cyan/Teal/Purple palette,
//  glassmorphism, floating orbs, skeleton reveals, bento grid,
//  gradient wave text, responsive mobile-first design.
// ═══════════════════════════════════════════════════════════


// ─── GLOBAL STYLES ─────────────────────────────────────────

dom.inject_css(`
  :root {
    --void: #06060a;
    --primary: #0a0a10;
    --secondary: #10101a;
    --deep: #000000;
    --cyan: #06b6d4;
    --teal: #14b8a6;
    --blue: #38bdf8;
    --purple: #a855f7;
    --indigo: #7c3aed;
    --glass-bg: rgba(16, 16, 26, 0.72);
    --glass-border: rgba(255, 255, 255, 0.08);
    --glass-shadow: 0 8px 32px rgba(0,0,0,0.4);
    --text-primary: #ffffff;
    --text-secondary: rgba(255,255,255,0.6);
    --text-dim: rgba(255,255,255,0.35);
    --font-mono: 'JetBrains Mono', monospace;
  }

  /* ── Scrollbar ── */
  ::-webkit-scrollbar { width: 6px; }
  ::-webkit-scrollbar-track { background: var(--void); }
  ::-webkit-scrollbar-thumb { background: var(--cyan); border-radius: 3px; }

  /* ── Gradient Text ── */
  .gradient-text {
    background: linear-gradient(135deg, var(--cyan), var(--teal), var(--purple));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    background-size: 200% 200%;
    animation: gradient-shift 4s ease infinite;
  }

  @keyframes gradient-shift {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  /* ── Glass Card ── */
  .glass {
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    border-radius: 1rem;
    box-shadow: var(--glass-shadow);
  }

  /* ── Orbs ── */
  .orb {
    position: fixed;
    border-radius: 50%;
    filter: blur(100px);
    opacity: 0.12;
    pointer-events: none;
    z-index: 0;
  }

  @keyframes orb-float {
    0%, 100% { transform: translate(0, 0) scale(1); }
    25% { transform: translate(30px, -25px) scale(1.05); }
    50% { transform: translate(-15px, 20px) scale(0.95); }
    75% { transform: translate(20px, 10px) scale(1.02); }
  }

  /* ── Reveal Animation ── */
  .reveal {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity 0.7s cubic-bezier(0.16, 1, 0.3, 1), transform 0.7s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* ── Section Spacing ── */
  section {
    position: relative;
    z-index: 1;
    padding: 6rem 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  /* ── Nav ── */
  .nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    padding: 1rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: rgba(6, 6, 10, 0.85);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--glass-border);
    transition: all 0.3s ease;
  }
  .nav-links { display: flex; gap: 2rem; list-style: none; }
  .nav-links a {
    color: var(--text-secondary);
    text-decoration: none;
    font-size: 0.9rem;
    font-weight: 500;
    transition: color 0.2s;
    letter-spacing: 0.02em;
  }
  .nav-links a:hover { color: var(--cyan); }
  .nav-logo {
    font-size: 1.1rem;
    font-weight: 800;
    letter-spacing: 0.15em;
    text-transform: uppercase;
  }
  .hamburger {
    display: none;
    background: none;
    border: none;
    color: var(--cyan);
    font-size: 1.5rem;
    cursor: pointer;
  }
  .mobile-menu {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 99;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    background: rgba(6, 6, 10, 0.95);
    backdrop-filter: blur(30px);
    -webkit-backdrop-filter: blur(30px);
  }
  .mobile-menu.open { display: flex; }
  .mobile-menu a {
    color: var(--text-primary);
    text-decoration: none;
    font-size: 1.5rem;
    font-weight: 600;
  }

  /* ── Hero ── */
  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding-top: 5rem;
  }
  .hero h1 {
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    font-weight: 900;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    letter-spacing: -0.02em;
  }
  .hero .subtitle {
    font-size: clamp(1rem, 2vw, 1.35rem);
    color: var(--text-secondary);
    max-width: 640px;
    line-height: 1.7;
    margin-bottom: 2.5rem;
  }
  .hero .tagline {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    color: var(--cyan);
    letter-spacing: 0.2em;
    text-transform: uppercase;
    margin-bottom: 3rem;
    opacity: 0.8;
  }

  /* ── Built With Badge ── */
  .built-with {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1.25rem;
    border: 1px solid rgba(6, 182, 212, 0.3);
    border-radius: 2rem;
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--cyan);
    background: rgba(6, 182, 212, 0.06);
    margin-bottom: 2rem;
  }
  .built-with .dot {
    width: 6px;
    height: 6px;
    background: var(--cyan);
    border-radius: 50%;
    animation: pulse 2s ease infinite;
  }
  @keyframes pulse {
    0%, 100% { opacity: 1; box-shadow: 0 0 0 0 rgba(6,182,212,0.4); }
    50% { opacity: 0.6; box-shadow: 0 0 0 6px rgba(6,182,212,0); }
  }

  /* ── Section Headers ── */
  .section-label {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--cyan);
    letter-spacing: 0.25em;
    text-transform: uppercase;
    margin-bottom: 1rem;
    opacity: 0.7;
  }
  .section-title {
    font-size: clamp(1.8rem, 4vw, 2.8rem);
    font-weight: 800;
    line-height: 1.2;
    margin-bottom: 1rem;
  }
  .section-desc {
    font-size: 1.1rem;
    color: var(--text-secondary);
    max-width: 600px;
    line-height: 1.7;
    margin-bottom: 3rem;
  }

  /* ── Bento Grid ── */
  .bento {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
    margin-top: 2rem;
  }
  .bento-card {
    padding: 2rem;
    border-radius: 1rem;
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    box-shadow: var(--glass-shadow);
    transition: transform 0.3s ease, border-color 0.3s ease;
    cursor: default;
  }
  .bento-card:hover {
    transform: translateY(-4px);
    border-color: rgba(6, 182, 212, 0.2);
  }
  .bento-card.span-2 { grid-column: span 2; }
  .bento-card .icon {
    font-size: 2rem;
    margin-bottom: 1rem;
    display: block;
  }
  .bento-card h3 {
    font-size: 1.15rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
  }
  .bento-card p {
    font-size: 0.92rem;
    color: var(--text-secondary);
    line-height: 1.6;
  }
  .bento-card .tag {
    display: inline-block;
    padding: 0.2rem 0.6rem;
    border-radius: 0.25rem;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    margin-top: 1rem;
    background: rgba(6, 182, 212, 0.1);
    color: var(--cyan);
    border: 1px solid rgba(6, 182, 212, 0.2);
  }

  /* ── Code Block ── */
  .code-block {
    background: var(--primary);
    border: 1px solid var(--glass-border);
    border-radius: 0.75rem;
    padding: 1.5rem;
    font-family: var(--font-mono);
    font-size: 0.85rem;
    line-height: 1.7;
    overflow-x: auto;
    color: var(--text-secondary);
    margin-top: 1rem;
  }
  .code-block .kw { color: var(--purple); }
  .code-block .fn { color: var(--cyan); }
  .code-block .str { color: var(--teal); }
  .code-block .cm { color: var(--text-dim); }

  /* ── Stats Row ── */
  .stats {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
    margin: 4rem 0;
  }
  .stat-card {
    text-align: center;
    padding: 2rem 1rem;
  }
  .stat-card .number {
    font-size: 2.5rem;
    font-weight: 900;
    margin-bottom: 0.5rem;
  }
  .stat-card .label {
    font-size: 0.85rem;
    color: var(--text-secondary);
    letter-spacing: 0.05em;
  }

  /* ── Carousel ── */
  .carousel {
    position: relative;
    overflow: hidden;
    border-radius: 1rem;
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
  }
  .carousel-track {
    display: flex;
    transition: transform 0.6s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .carousel-slide {
    min-width: 100%;
    padding: 3rem;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
  }
  .carousel-slide .slide-content h3 {
    font-size: 1.6rem;
    font-weight: 700;
    margin-bottom: 1rem;
  }
  .carousel-slide .slide-content p {
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: 1.5rem;
  }
  .carousel-dots {
    display: flex;
    justify-content: center;
    gap: 0.5rem;
    padding: 1.5rem;
  }
  .carousel-dot {
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: none;
    border: none;
    cursor: pointer;
  }
  .carousel-dot::before {
    content: '';
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--text-dim);
    transition: all 0.3s ease;
  }
  .carousel-dot.active::before {
    background: var(--cyan);
    box-shadow: 0 0 12px rgba(6, 182, 212, 0.5);
    width: 24px;
    border-radius: 4px;
  }

  /* ── Footer ── */
  .footer {
    background: var(--deep);
    border-top: 1px solid var(--glass-border);
    padding: 4rem 2rem 2rem;
    margin-top: 4rem;
  }
  .footer-grid {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr 1fr;
    gap: 3rem;
    max-width: 1200px;
    margin: 0 auto;
  }
  .footer h4 {
    font-size: 0.8rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--text-secondary);
    margin-bottom: 1rem;
  }
  .footer a {
    display: block;
    color: var(--text-dim);
    text-decoration: none;
    font-size: 0.9rem;
    padding: 0.3rem 0;
    transition: color 0.2s;
  }
  .footer a:hover { color: var(--cyan); }
  .footer-brand {
    font-size: 0.9rem;
    color: var(--text-dim);
    line-height: 1.7;
  }
  .footer-bottom {
    text-align: center;
    padding-top: 3rem;
    margin-top: 3rem;
    border-top: 1px solid rgba(255,255,255,0.05);
    font-size: 0.8rem;
    color: var(--text-dim);
    max-width: 1200px;
    margin-left: auto;
    margin-right: auto;
  }

  /* ── Responsive ── */
  @media (max-width: 1024px) {
    .bento { grid-template-columns: repeat(2, 1fr); }
    .bento-card.span-2 { grid-column: span 1; }
    .stats { grid-template-columns: repeat(2, 1fr); }
    .footer-grid { grid-template-columns: 1fr 1fr; }
  }
  @media (max-width: 768px) {
    section { padding: 4rem 1.25rem; }
    .nav-links { display: none; }
    .hamburger { display: block; }
    .carousel-slide { grid-template-columns: 1fr; }
    .footer-grid { grid-template-columns: 1fr; gap: 2rem; }
  }
  @media (max-width: 480px) {
    .bento { grid-template-columns: 1fr; }
    .stats { grid-template-columns: 1fr; }
    .hero h1 { font-size: 2rem; }
    .carousel-slide { padding: 2rem 1.5rem; }
  }
`, "dwsc-styles")


// ─── FLOATING ORBS ─────────────────────────────────────────

let orb1 = dom.create("div", {
  className: "orb",
  styles: { width: "500px", height: "500px", background: "radial-gradient(circle, #06b6d4 0%, transparent 70%)", top: "5%", left: "10%", animation: "orb-float 12s ease-in-out infinite" }
})
dom.mount(orb1)

let orb2 = dom.create("div", {
  className: "orb",
  styles: { width: "400px", height: "400px", background: "radial-gradient(circle, #a855f7 0%, transparent 70%)", top: "40%", right: "5%", animation: "orb-float 15s ease-in-out infinite reverse" }
})
dom.mount(orb2)

let orb3 = dom.create("div", {
  className: "orb",
  styles: { width: "350px", height: "350px", background: "radial-gradient(circle, #14b8a6 0%, transparent 70%)", bottom: "10%", left: "30%", animation: "orb-float 10s ease-in-out infinite 2s" }
})
dom.mount(orb3)



// ─── NAVIGATION ────────────────────────────────────────────

let mobile_open = state.reactive(false)

let hamburger = dom.create("button", {
  className: "hamburger",
  html: "&#9776;",
  attrs: { "aria-label": "Menu" },
  onClick: () => {
    mobile_open.set(!mobile_open.get())
    let menu = dom.select(".mobile-menu")
    if (mobile_open.get()) {
      dom.add_class(menu, "open")
      hamburger.innerHTML = "&#10005;"
    } else {
      dom.remove_class(menu, "open")
      hamburger.innerHTML = "&#9776;"
    }
  }
})

let nav = dom.create("nav", {
  className: "nav",
  children: [
    dom.create("div", {
      className: "nav-logo gradient-text",
      text: "DWSC"
    }),
    dom.create("ul", {
      className: "nav-links",
      children: [
        dom.create("li", { children: [dom.create("a", { text: "Research", attrs: { href: "#research" } })] }),
        dom.create("li", { children: [dom.create("a", { text: "Ecosystem", attrs: { href: "#ecosystem" } })] }),
        dom.create("li", { children: [dom.create("a", { text: "Lume", attrs: { href: "#lume" } })] }),
        dom.create("li", { children: [dom.create("a", { text: "Roadmap", attrs: { href: "#roadmap" } })] }),
        dom.create("li", { children: [dom.create("a", { text: "Papers", attrs: { href: "#papers" } })] }),
        dom.create("li", { children: [dom.create("a", { text: "Blog", attrs: { href: "#blog" } })] }),
        dom.create("li", { children: [dom.create("a", { text: "Contact", attrs: { href: "#contact" } })] })
      ]
    }),
    hamburger
  ]
})
dom.mount(nav)

let mobile_menu = dom.create("div", {
  className: "mobile-menu",
  children: [
    dom.create("a", { text: "Research", attrs: { href: "#research" }, onClick: () => { mobile_open.set(false); dom.remove_class(dom.select(".mobile-menu"), "open"); hamburger.innerHTML = "&#9776;" } }),
    dom.create("a", { text: "Ecosystem", attrs: { href: "#ecosystem" }, onClick: () => { mobile_open.set(false); dom.remove_class(dom.select(".mobile-menu"), "open"); hamburger.innerHTML = "&#9776;" } }),
    dom.create("a", { text: "Lume", attrs: { href: "#lume" }, onClick: () => { mobile_open.set(false); dom.remove_class(dom.select(".mobile-menu"), "open"); hamburger.innerHTML = "&#9776;" } }),
    dom.create("a", { text: "Playground", attrs: { href: "#playground" }, onClick: () => { mobile_open.set(false); dom.remove_class(dom.select(".mobile-menu"), "open"); hamburger.innerHTML = "&#9776;" } }),
    dom.create("a", { text: "Verticals", attrs: { href: "#verticals" }, onClick: () => { mobile_open.set(false); dom.remove_class(dom.select(".mobile-menu"), "open"); hamburger.innerHTML = "&#9776;" } }),
    dom.create("a", { text: "Papers", attrs: { href: "#papers" }, onClick: () => { mobile_open.set(false); dom.remove_class(dom.select(".mobile-menu"), "open"); hamburger.innerHTML = "&#9776;" } }),
    dom.create("a", { text: "Contact", attrs: { href: "#contact" }, onClick: () => { mobile_open.set(false); dom.remove_class(dom.select(".mobile-menu"), "open"); hamburger.innerHTML = "&#9776;" } })
  ]
})
dom.mount(mobile_menu)



// ─── HERO SECTION ──────────────────────────────────────────

dom.inject_css(`
  .hero-cta {
    display: flex;
    gap: 1rem;
    margin-top: 2.5rem;
    flex-wrap: wrap;
    justify-content: center;
  }
  .hero-cta a {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.85rem 2rem;
    border-radius: 0.75rem;
    font-size: 0.95rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    letter-spacing: 0.02em;
  }
  .hero-cta .cta-primary {
    background: linear-gradient(135deg, var(--cyan), var(--teal));
    color: var(--void);
    box-shadow: 0 4px 24px rgba(6, 182, 212, 0.3);
  }
  .hero-cta .cta-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 32px rgba(6, 182, 212, 0.5);
  }
  .hero-cta .cta-secondary {
    background: transparent;
    color: var(--cyan);
    border: 1px solid rgba(6, 182, 212, 0.3);
  }
  .hero-cta .cta-secondary:hover {
    border-color: var(--cyan);
    background: rgba(6, 182, 212, 0.08);
    transform: translateY(-2px);
  }
  @media (max-width: 480px) {
    .hero-cta { flex-direction: column; align-items: stretch; }
    .hero-cta a { justify-content: center; }
  }
`, "dwsc-hero-cta-styles")

let hero = dom.create("section", {
  className: "hero",
  children: [
    dom.create("div", {
      className: "built-with reveal",
      children: [
        dom.create("span", { className: "dot" }),
        dom.create("span", { text: "Built with Lume" })
      ]
    }),
    dom.create("h1", {
      className: "gradient-text reveal",
      text: "DarkWave Systems Collective"
    }),
    dom.create("p", {
      className: "subtitle reveal",
      text: "Research & Engineering Division of DarkWave Studios LLC. Pioneering the Trust Layer ecosystem and Lume — the AI-native programming language that eliminates cognitive distance."
    }),
    dom.create("div", {
      className: "tagline reveal",
      text: "Intent → Code → Certified. Zero translation."
    }),
    dom.create("div", {
      className: "hero-cta reveal",
      children: [
        dom.create("a", {
          className: "cta-primary",
          text: "◈ Try Lume IDE at TrustGen",
          attrs: { href: "https://trustgen.tlid.io", target: "_blank" }
        }),
        dom.create("a", {
          className: "cta-secondary",
          text: "📄 Read the Paper",
          attrs: { href: "#papers" }
        }),
        dom.create("a", {
          className: "cta-secondary",
          text: "🔬 Explore Ecosystem",
          attrs: { href: "#ecosystem" }
        })
      ]
    })
  ]
})
dom.mount(hero, "#app")


// ─── STATS ─────────────────────────────────────────────────

let stats_section = dom.create("section", {
  children: [
    dom.create("div", {
      className: "stats",
      children: [
        dom.create("div", { className: "stat-card glass reveal", children: [
          dom.create("div", { className: "number gradient-text", text: "7" }),
          dom.create("div", { className: "label", text: "Tolerance Layers" })
        ]}),
        dom.create("div", { className: "stat-card glass reveal", children: [
          dom.create("div", { className: "number gradient-text", text: "CI ✔" }),
          dom.create("div", { className: "label", text: "Build Verified" })
        ]}),
        dom.create("div", { className: "stat-card glass reveal", children: [
          dom.create("div", { className: "number gradient-text", text: "3" }),
          dom.create("div", { className: "label", text: "Security Layers" })
        ]}),
        dom.create("div", { className: "stat-card glass reveal", children: [
          dom.create("div", { className: "number gradient-text", text: "0" }),
          dom.create("div", { className: "label", text: "~0 Cognitive Distance" })
        ]})
      ]
    })
  ]
})
dom.mount(stats_section, "#app")



// ─── LUME SECTION ──────────────────────────────────────────

let lume_section = dom.create("section", {
  id: "lume",
  children: [
    dom.create("div", { className: "section-label reveal", text: "// FLAGSHIP" }),
    dom.create("h2", { className: "section-title gradient-text reveal", text: "Lume Programming Language" }),
    dom.create("p", { className: "section-desc reveal", text: "The world's first AI-native programming language. Write in English. Compile with voice. Ship with confidence. Every line is security-certified at birth." }),
    dom.create("div", {
      className: "code-block glass reveal",
      html: '<span class="cm">// English Mode — zero cognitive distance</span>\n<span class="kw">get</span> all the users who signed up this month\n<span class="kw">show</span> their names and emails\n<span class="kw">sort</span> them by signup date descending\n\n<span class="cm">// Compiles to certified JavaScript:</span>\n<span class="kw">const</span> result = <span class="kw">await</span> <span class="fn">db.query</span>(<span class="str">\'SELECT name, email FROM users\n  WHERE created_at >= DATE_TRUNC(month, CURRENT_DATE)\n  ORDER BY created_at DESC\'</span>);\n<span class="cm">// LUME-CERT: sha256:a3f8... | Intent: QUERY | Risk: LOW</span>'
    })
  ]
})
dom.mount(lume_section, "#app")


// ─── LUME LANGUAGE HIGHLIGHT ────────────────────────────────

dom.inject_css(`
  .lume-highlight {
    padding: 4rem 2rem;
    text-align: center;
    position: relative;
  }
  .highlight-card {
    max-width: 800px;
    margin: 0 auto;
    padding: 3rem;
    border-radius: 1.25rem;
    background: linear-gradient(135deg, rgba(6, 182, 212, 0.04), rgba(168, 85, 247, 0.03));
    border: 1px solid rgba(6, 182, 212, 0.15);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4), 0 0 60px rgba(6, 182, 212, 0.04);
  }
  .highlight-card h3 {
    font-size: 1.75rem;
    font-weight: 800;
    margin-bottom: 0.75rem;
    letter-spacing: -0.5px;
    line-height: 1.2;
  }
  .highlight-card p {
    font-size: 1rem;
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: 1.5rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }
  .highlight-stats {
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin-bottom: 2rem;
    flex-wrap: wrap;
  }
  .highlight-stat {
    text-align: center;
    padding: 1rem 1.5rem;
    border-radius: 0.75rem;
    background: rgba(6, 182, 212, 0.06);
    border: 1px solid rgba(6, 182, 212, 0.12);
  }
  .highlight-stat .stat-val {
    font-size: 1.75rem;
    font-weight: 900;
    font-family: var(--font-mono);
    line-height: 1;
    margin-bottom: 0.25rem;
  }
  .highlight-stat .stat-lbl {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-secondary);
  }
  .highlight-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.85rem 2rem;
    border-radius: 0.5rem;
    background: linear-gradient(135deg, var(--cyan), var(--teal));
    color: #fff;
    font-size: 0.95rem;
    font-weight: 700;
    text-decoration: none;
    border: none;
    cursor: pointer;
    transition: transform 0.3s, box-shadow 0.3s;
    box-shadow: 0 4px 20px rgba(6, 182, 212, 0.3);
  }
  .highlight-cta:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(6, 182, 212, 0.4);
  }
  @media (max-width: 768px) {
    .highlight-stats { gap: 1rem; }
    .highlight-stat { padding: 0.75rem 1rem; }
    .highlight-card { padding: 2rem 1.5rem; }
  }
`, "dwsc-lume-highlight-styles")

let lume_highlight = dom.create("section", {
  id: "lume-highlight",
  children: [
    dom.create("div", { className: "highlight-card reveal", children: [
      dom.create("h3", { className: "gradient-text", text: "The Lume Programming Language" }),
      dom.create("p", { text: "Voice-to-code compilation, English Mode, 7-layer tolerance, and certified-at-birth security. Explore the interactive playground, test dashboard, and vertical applications on the language site." }),
      dom.create("div", { className: "highlight-stats", children: [
        dom.create("div", { className: "highlight-stat", children: [
          dom.create("div", { className: "stat-val gradient-text", text: "CI ✔" }),
          dom.create("div", { className: "stat-lbl", text: "Build Verified" })
        ]}),
        dom.create("div", { className: "highlight-stat", children: [
          dom.create("div", { className: "stat-val gradient-text", text: "102" }),
          dom.create("div", { className: "stat-lbl", text: "English Patterns" })
        ]}),
        dom.create("div", { className: "highlight-stat", children: [
          dom.create("div", { className: "stat-val gradient-text", text: "3-Stage" }),
          dom.create("div", { className: "stat-lbl", text: "Validation Pipeline" })
        ]})
      ]}),
      dom.create("a", { className: "highlight-cta", text: "Explore Lume →", attrs: { href: "https://lume-lang.com", target: "_blank", rel: "noopener" } })
    ]})
  ]
})
dom.mount(lume_highlight, "#app")



// ─── RESEARCH CAROUSEL ─────────────────────────────────────

let current_slide = state.reactive(0)
const slide_count = 3

let slides_data = [
  {
    title: "Cognitive Distance Theory",
    desc: "A formal mathematical framework proving that programming difficulty is proportional to the gap between human intent and machine syntax. Lume's CD score approaches zero.",
    badge: "§8.1 — Theoretical Contribution"
  },
  {
    title: "Certified-at-Birth Security",
    desc: "Every compiled line carries a SHA-256 certificate binding Input + AST + JavaScript. The Semantic Invariant Test ensures intent-to-execution integrity is cryptographically provable.",
    badge: "§8.13 — Three-Layer Security"
  },
  {
    title: "Auditory Mode",
    desc: "The first programming language usable with eyes closed. Speak your intent, hear the compiler's understanding, confirm by voice. Full bidirectional speech pipeline — zero screens, zero keyboards.",
    badge: "§8.14 — Accessibility as Architecture"
  }
]

let track = dom.create("div", { className: "carousel-track" })

for (const slide of slides_data) {
  let slide_el = dom.create("div", {
    className: "carousel-slide",
    children: [
      dom.create("div", {
        className: "slide-content",
        children: [
          dom.create("h3", { className: "gradient-text", text: slide.title }),
          dom.create("p", { text: slide.desc }),
          dom.create("span", { className: "tag", text: slide.badge })
        ]
      }),
      dom.create("div", {
        className: "code-block",
        styles: { fontSize: "0.8rem" },
        html: '<span class="cm">// Research paper section reference</span>\n<span class="kw">define</span> contribution = <span class="str">"' + slide.title + '"</span>\n<span class="kw">show</span> contribution.impact'
      })
    ]
  })
  dom.add_child(track, slide_el)
}

let dots_container = dom.create("div", { className: "carousel-dots" })

for (let i = 0; i < slide_count; i++) {
  let dot = dom.create("button", {
    className: "carousel-dot" + (i === 0 ? " active" : ""),
    attrs: { "aria-label": "Slide " + (i + 1) }
  })
  dom.on(dot, "click", () => {
    current_slide.set(i)
  })
  dom.add_child(dots_container, dot)
}

current_slide.on_change((val) => {
  track.style.transform = "translateX(-" + (val * 100) + "%)"
  let dots = dom.select_all(".carousel-dot")
  for (const d of dots) {
    dom.remove_class(d, "active")
  dom.add_class(dots[val], "active")
  }
})

// Auto-rotate
let auto_rotate = setInterval(() => {
  let next = (current_slide.get() + 1) % slide_count
  current_slide.set(next)
}, 6000)

let research_section = dom.create("section", {
  id: "research",
  children: [
    dom.create("div", { className: "section-label reveal", text: "// RESEARCH" }),
    dom.create("h2", { className: "section-title gradient-text reveal", text: "Academic Contributions" }),
    dom.create("p", { className: "section-desc reveal", text: "Peer-reviewable theoretical frameworks, experimental designs, and novel paradigms in programming language design." }),
    dom.create("div", {
      className: "carousel reveal",
      children: [track, dots_container]
    })
  ]
})
dom.mount(research_section, "#app")



// ─── ECOSYSTEM (FOCUSED) ───────────────────────────────────

dom.inject_css(`
  .eco-stats {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
    margin-bottom: 3rem;
  }
  .eco-stat {
    text-align: center;
    padding: 1.5rem 1rem;
    border-radius: 1rem;
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
  }
  .eco-stat .eco-num {
    font-size: 2rem;
    font-weight: 900;
    margin-bottom: 0.3rem;
  }
  .eco-stat .eco-label {
    font-size: 0.8rem;
    color: var(--text-secondary);
    letter-spacing: 0.06em;
  }
  .eco-flagships {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.25rem;
    margin-bottom: 2.5rem;
  }
  .eco-flagship {
    padding: 1.5rem;
    border-radius: 1rem;
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    box-shadow: var(--glass-shadow);
    transition: transform 0.3s ease, border-color 0.3s ease;
    text-align: center;
  }
  .eco-flagship:hover {
    transform: translateY(-4px);
    border-color: rgba(6, 182, 212, 0.25);
  }
  .eco-flagship .eco-icon {
    font-size: 2rem;
    margin-bottom: 0.75rem;
    display: block;
  }
  .eco-flagship h3 {
    font-size: 1rem;
    font-weight: 700;
    margin-bottom: 0.4rem;
  }
  .eco-flagship p {
    font-size: 0.82rem;
    color: var(--text-secondary);
    line-height: 1.5;
  }
  .eco-flagship .eco-tag {
    display: inline-block;
    padding: 0.15rem 0.5rem;
    border-radius: 0.25rem;
    font-family: var(--font-mono);
    font-size: 0.65rem;
    margin-top: 0.75rem;
    background: rgba(6, 182, 212, 0.1);
    color: var(--cyan);
    border: 1px solid rgba(6, 182, 212, 0.2);
  }
  .eco-cta-row {
    text-align: center;
    margin-top: 2rem;
  }
  .eco-cta-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.8rem 2rem;
    border-radius: 2rem;
    background: rgba(6, 182, 212, 0.06);
    border: 1px solid rgba(6, 182, 212, 0.2);
    color: var(--cyan);
    text-decoration: none;
    font-size: 0.9rem;
    font-weight: 600;
    transition: all 0.3s ease;
    letter-spacing: 0.02em;
  }
  .eco-cta-link:hover {
    background: rgba(6, 182, 212, 0.12);
    border-color: rgba(6, 182, 212, 0.4);
    transform: translateY(-2px);
  }
  @media (max-width: 768px) {
    .eco-stats { grid-template-columns: repeat(2, 1fr); }
    .eco-flagships { grid-template-columns: 1fr 1fr; }
  }
  @media (max-width: 480px) {
    .eco-flagships { grid-template-columns: 1fr; }
  }
`, "dwsc-eco-focused-styles")

let ecosystem_section = dom.create("section", {
  id: "ecosystem",
  children: [
    dom.create("div", { className: "section-label reveal", text: "// ECOSYSTEM" }),
    dom.create("h2", { className: "section-title gradient-text reveal", text: "Trust Layer Ecosystem" }),
    dom.create("p", { className: "section-desc reveal", text: "Lume powers a production ecosystem of 38+ interconnected applications — all unified by Trust Layer SSO and blockchain-verified identity." }),

    // Stats row
    dom.create("div", {
      className: "eco-stats reveal",
      children: [
        dom.create("div", { className: "eco-stat", children: [
          dom.create("div", { className: "eco-num gradient-text", text: "38+" }),
          dom.create("div", { className: "eco-label", text: "Production Apps" })
        ]}),
        dom.create("div", { className: "eco-stat", children: [
          dom.create("div", { className: "eco-num gradient-text", text: "8" }),
          dom.create("div", { className: "eco-label", text: "Industry Verticals" })
        ]}),
        dom.create("div", { className: "eco-stat", children: [
          dom.create("div", { className: "eco-num gradient-text", text: "1" }),
          dom.create("div", { className: "eco-label", text: "Unified SSO" })
        ]}),
        dom.create("div", { className: "eco-stat", children: [
          dom.create("div", { className: "eco-num gradient-text", text: "✦" }),
          dom.create("div", { className: "eco-label", text: "Lume Powered" })
        ]})
      ]
    }),

    // Flagship apps only
    dom.create("div", {
      className: "eco-flagships",
      children: [
        dom.create("div", { className: "eco-flagship reveal", children: [
          dom.create("span", { className: "eco-icon", text: "🛡️" }),
          dom.create("h3", { text: "Trust Layer Hub" }),
          dom.create("p", { text: "Central SSO, hallmarks, and cross-app authentication." }),
          dom.create("span", { className: "eco-tag", text: "CORE" })
        ]}),
        dom.create("div", { className: "eco-flagship reveal", children: [
          dom.create("span", { className: "eco-icon", text: "◈" }),
          dom.create("h3", { text: "TrustGen + Lume IDE" }),
          dom.create("p", { text: "AI-powered 3D studio with built-in Lume editor and voice direction." }),
          dom.create("span", { className: "eco-tag", text: "DEVELOPER" })
        ]}),
        dom.create("div", { className: "eco-flagship reveal", children: [
          dom.create("span", { className: "eco-icon", text: "🔗" }),
          dom.create("h3", { text: "Signal Chat" }),
          dom.create("p", { text: "Real-time messaging with bot framework and WebSocket protocol." }),
          dom.create("span", { className: "eco-tag", text: "COMMUNICATION" })
        ]}),
        dom.create("div", { className: "eco-flagship reveal", children: [
          dom.create("span", { className: "eco-icon", text: "🏗️" }),
          dom.create("h3", { text: "DarkWave Studio" }),
          dom.create("p", { text: "Ecosystem IDE with Lume toolchain, debugging, and live preview." }),
          dom.create("span", { className: "eco-tag", text: "DEVELOPER" })
        ]})
      ]
    }),

    // CTA to full directory
    dom.create("div", {
      className: "eco-cta-row reveal",
      children: [
        dom.create("a", {
          className: "eco-cta-link",
          text: "View Full Ecosystem Directory →",
          attrs: { href: "https://dwtl.io", target: "_blank" }
        })
      ]
    })
  ]
})
dom.mount(ecosystem_section, "#app")


// ─── ECOSYSTEM ARCHITECTURE DIAGRAM ────────────────────────

dom.inject_css(`
  .arch-container {
    position: relative;
    width: 100%;
    max-width: 700px;
    margin: 0 auto;
    aspect-ratio: 1;
  }
  .arch-hub {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 120px;
    height: 120px;
    border-radius: 50%;
    background: linear-gradient(135deg, rgba(6,182,212,0.15), rgba(168,85,247,0.1));
    border: 2px solid rgba(6,182,212,0.3);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    z-index: 10;
    box-shadow: 0 0 40px rgba(6,182,212,0.15);
  }
  .arch-hub-icon { font-size: 1.5rem; margin-bottom: 0.25rem; }
  .arch-hub-label { font-size: 0.65rem; font-weight: 700; color: var(--cyan); text-transform: uppercase; letter-spacing: 0.1em; text-align: center; }
  .arch-ring {
    position: absolute;
    top: 50%;
    left: 50%;
    border: 1px solid rgba(6,182,212,0.08);
    border-radius: 50%;
    transform: translate(-50%, -50%);
  }
  .arch-node {
    position: absolute;
    width: 56px;
    height: 56px;
    border-radius: 50%;
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    cursor: pointer;
    transition: all 0.3s ease;
    z-index: 5;
  }
  .arch-node:hover {
    border-color: rgba(6,182,212,0.4);
    transform: scale(1.15);
    box-shadow: 0 0 20px rgba(6,182,212,0.2);
  }
  .arch-node .arch-tooltip {
    position: absolute;
    bottom: calc(100% + 8px);
    left: 50%;
    transform: translateX(-50%);
    padding: 0.4rem 0.75rem;
    border-radius: 0.5rem;
    background: var(--primary);
    border: 1px solid rgba(6,182,212,0.2);
    white-space: nowrap;
    font-size: 0.7rem;
    color: var(--text-primary);
    font-weight: 600;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.2s;
  }
  .arch-node:hover .arch-tooltip { opacity: 1; }
  @keyframes orbit-pulse {
    0%, 100% { opacity: 0.08; }
    50% { opacity: 0.15; }
  }
  @media (max-width: 768px) {
    .arch-container { max-width: 360px; }
    .arch-hub { width: 80px; height: 80px; }
    .arch-hub-icon { font-size: 1.1rem; }
    .arch-hub-label { font-size: 0.5rem; }
    .arch-node { width: 36px; height: 36px; font-size: 0.8rem; }
  }
  @media (max-width: 480px) {
    .arch-container { max-width: 280px; }
    .arch-hub { width: 64px; height: 64px; }
    .arch-node { width: 30px; height: 30px; font-size: 0.7rem; }
  }
`, "dwsc-arch-styles")

// Build the hub-and-spoke nodes
let arch_apps = [
  { icon: "🔐", name: "Trust Layer SSO", cat: "core" },
  { icon: "◈", name: "TrustGen 3D", cat: "dev" },
  { icon: "🎵", name: "DarkWave Studio", cat: "dev" },
  { icon: "💬", name: "Signal Chat", cat: "social" },
  { icon: "🏗️", name: "DW Studio IDE", cat: "dev" },
  { icon: "🎓", name: "DarkWave Academy", cat: "edu" },
  { icon: "💎", name: "Lume Lang", cat: "dev" },
  { icon: "🎮", name: "Bomber 3D", cat: "game" },
  { icon: "🌳", name: "Arbora", cat: "social" },
  { icon: "🎲", name: "Chronicles", cat: "game" },
  { icon: "🍺", name: "BrewBoard", cat: "comm" },
  { icon: "🎨", name: "PaintPros", cat: "comm" },
  { icon: "🏠", name: "TrustHome", cat: "comm" },
  { icon: "📊", name: "Verdara", cat: "ent" },
  { icon: "🌿", name: "Bloom", cat: "social" },
  { icon: "📰", name: "Newsroom", cat: "social" },
  { icon: "🔧", name: "Deploy Engine", cat: "infra" },
  { icon: "✅", name: "Verify Keyword", cat: "infra" },
  { icon: "🔗", name: "Hallmarks", cat: "core" },
  { icon: "📝", name: "Trust Stamps", cat: "core" },
  { icon: "🍔", name: "Happy Eats", cat: "comm" }
]

let arch_container = dom.create("div", { className: "arch-container" })

// Hub
dom.add_child(arch_container, dom.create("div", {
  className: "arch-hub",
  children: [
    dom.create("div", { className: "arch-hub-icon", text: "🛡️" }),
    dom.create("div", { className: "arch-hub-label", text: "Trust Layer" })
  ]
}))

// Rings
let ring_sizes = [240, 340, 440]
for (const size of ring_sizes) {
  dom.add_child(arch_container, dom.create("div", {
    className: "arch-ring",
    styles: { width: size + "px", height: size + "px", animation: "orbit-pulse 4s ease infinite " + (size / 100) + "s" }
  }))
}

// Position nodes around rings
for (let i = 0; i < arch_apps.length; i++) {
  let app = arch_apps[i]
  let ring = i < 6 ? 0 : (i < 13 ? 1 : 2)
  let ring_radius = ring_sizes[ring] / 2
  let items_on_ring = ring === 0 ? 6 : (ring === 1 ? 7 : 7)
  let idx_on_ring = ring === 0 ? i : (ring === 1 ? i - 6 : i - 13)
  let angle = (idx_on_ring / items_on_ring) * 2 * 3.14159 - 1.5708
  let cx = 50 + (ring_radius / 3.5) * Math.cos(angle)
  let cy = 50 + (ring_radius / 3.5) * Math.sin(angle)

  let node = dom.create("div", {
    className: "arch-node",
    styles: { left: cx + "%", top: cy + "%", transform: "translate(-50%, -50%)" },
    children: [
      dom.create("span", { text: app.icon }),
      dom.create("span", { className: "arch-tooltip", text: app.name })
    ]
  })
  dom.add_child(arch_container, node)
}

let arch_section = dom.create("section", {
  id: "architecture",
  children: [
    dom.create("div", { className: "section-label reveal", text: "// ARCHITECTURE" }),
    dom.create("h2", { className: "section-title gradient-text reveal", text: "Ecosystem Architecture" }),
    dom.create("p", { className: "section-desc reveal", text: "All applications connect through Trust Layer SSO — one identity, one authentication, one ecosystem." }),
    dom.create("div", { className: "reveal", children: [arch_container] })
  ]
})
dom.mount(arch_section, "#app")


// ─── CATEGORIZED ECOSYSTEM CAROUSELS ───────────────────────

dom.inject_css(`
  .cat-tabs {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
    margin-bottom: 1.5rem;
  }
  .cat-tab {
    padding: 0.5rem 1.25rem;
    border-radius: 2rem;
    border: 1px solid var(--glass-border);
    background: transparent;
    color: var(--text-secondary);
    font-size: 0.8rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    letter-spacing: 0.02em;
  }
  .cat-tab:hover { border-color: rgba(6,182,212,0.3); color: var(--cyan); }
  .cat-tab.active {
    background: rgba(6,182,212,0.1);
    border-color: rgba(6,182,212,0.3);
    color: var(--cyan);
  }
  .cat-carousel {
    position: relative;
    overflow: hidden;
    border-radius: 1rem;
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
  }
  .cat-track {
    display: flex;
    transition: transform 0.6s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .cat-card {
    min-width: 50%;
    padding: 2rem;
    box-sizing: border-box;
    border-right: 1px solid var(--glass-border);
  }
  .cat-card-icon { font-size: 2rem; margin-bottom: 0.75rem; }
  .cat-card h4 { font-size: 1.05rem; font-weight: 700; margin-bottom: 0.4rem; }
  .cat-card p { font-size: 0.85rem; color: var(--text-secondary); line-height: 1.5; margin-bottom: 0.75rem; }
  .cat-card .cat-link {
    font-size: 0.75rem;
    color: var(--cyan);
    text-decoration: none;
    font-weight: 600;
    letter-spacing: 0.05em;
  }
  .cat-card .cat-link:hover { text-decoration: underline; }
  .cat-nav {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 1rem;
    padding: 1rem;
  }
  .cat-arrow {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    border: 1px solid var(--glass-border);
    background: transparent;
    color: var(--cyan);
    font-size: 1rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
  }
  .cat-arrow:hover { border-color: var(--cyan); background: rgba(6,182,212,0.08); }
  .cat-dots {
    display: flex;
    gap: 0.4rem;
  }
  .cat-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--text-dim);
    border: none;
    cursor: pointer;
    transition: all 0.3s ease;
    padding: 0;
  }
  .cat-dot.active {
    background: var(--cyan);
    box-shadow: 0 0 10px rgba(6,182,212,0.5);
    width: 20px;
    border-radius: 4px;
  }
  @media (max-width: 768px) {
    .cat-card { min-width: 100%; }
    .cat-tabs { justify-content: center; }
  }
`, "dwsc-category-styles")

let categories = [
  {
    name: "Developer Tools",
    apps: [
      { icon: "💎", name: "Lume Language", desc: "AI-native language with voice-to-code and certified security.", url: "https://lume-lang.com" },
      { icon: "◈", name: "TrustGen 3D", desc: "AI-powered 3D generation with built-in Lume IDE.", url: "https://trustgen.tlid.io" },
      { icon: "🏗️", name: "DarkWave Studio", desc: "Ecosystem IDE with Lume toolchain integration.", url: "https://darkwavestudio.tlid.io" }
    ]
  },
  {
    name: "Security & Identity",
    apps: [
      { icon: "🔐", name: "Trust Layer Hub", desc: "Central SSO, hallmarks, and blockchain identity.", url: "https://dwtl.io" }
    ]
  },
  {
    name: "Commerce & Services",
    apps: [
      { icon: "🎨", name: "PaintPros", desc: "Professional painting service marketplace.", url: "https://paintpros.tlid.io" },
      { icon: "🏠", name: "TrustHome", desc: "Real estate and property management platform.", url: "https://trusthome.tlid.io" },
      { icon: "🍺", name: "BrewBoard", desc: "Craft brewery discovery and review platform.", url: "https://brewboard.tlid.io" },
      { icon: "🍔", name: "Happy Eats", desc: "Last-mile food delivery for truckers and rural workers. Vendor portals, driver dispatch, Stripe payments.", url: "https://happyeats.app" }
    ]
  },
  {
    name: "Gaming & Entertainment",
    apps: [
      { icon: "🎮", name: "Bomber 3D", desc: "Long-drive golf game with Three.js physics.", url: "https://bomber3d.tlid.io" },
      { icon: "🎲", name: "Chronicles", desc: "Collaborative storytelling RPG platform.", url: "https://chronicles.tlid.io" },
      { icon: "🎵", name: "DarkWave Studio", desc: "Music production and audio engineering.", url: "https://darkwavestudio.tlid.io" }
    ]
  },
  {
    name: "Enterprise Operations",
    apps: [
      { icon: "📊", name: "Verdara", desc: "Enterprise analytics and reporting dashboard.", url: "https://verdara.tlid.io" }
    ]
  },
  {
    name: "Community & Social",
    apps: [
      { icon: "💬", name: "Signal Chat", desc: "Real-time messaging with bot framework.", url: "https://signalchat.tlid.io" },
      { icon: "🌳", name: "Arbora", desc: "Community tree planting and sustainability.", url: "https://arbora.tlid.io" },
      { icon: "🎓", name: "DarkWave Academy", desc: "Educational platform with tutorials.", url: "https://darkwaveacademy.tlid.io" }
    ]
  }
]

let active_cat = state.reactive(0)
let cat_positions = []

// Build tab buttons
let tab_container = dom.create("div", { className: "cat-tabs" })
for (let c = 0; c < categories.length; c++) {
  let tab_btn = dom.create("button", {
    className: "cat-tab" + (c === 0 ? " active" : ""),
    text: categories[c].name,
    attrs: { "data-cat-idx": c + "" }
  })
  dom.on(tab_btn, "click", () => {
    active_cat.set(c)
  })
  dom.add_child(tab_container, tab_btn)
}

// Build category carousel panels
let panels_wrapper = dom.create("div", { styles: { position: "relative" } })

for (let c = 0; c < categories.length; c++) {
  let cat = categories[c]
  let cat_slide_pos = state.reactive(0)
  cat_positions.push(cat_slide_pos)
  let slides_count = Math.ceil(cat.apps.length / 2)

  let cat_track = dom.create("div", { className: "cat-track" })
  for (let s = 0; s < slides_count; s++) {
    let slide = dom.create("div", { styles: { minWidth: "100%", display: "flex" } })
    for (let a = s * 2; a < Math.min((s + 1) * 2, cat.apps.length); a++) {
      let app = cat.apps[a]
      let card = dom.create("div", {
        className: "cat-card",
        children: [
          dom.create("div", { className: "cat-card-icon", text: app.icon }),
          dom.create("h4", { text: app.name }),
          dom.create("p", { text: app.desc }),
          dom.create("a", { className: "cat-link", text: app.url !== "#" ? "Visit →" : "Coming Soon", attrs: { href: app.url, target: "_blank" } })
        ]
      })
      dom.add_child(slide, card)
    }
    dom.add_child(cat_track, slide)
  }

  // Dots
  let dots_div = dom.create("div", { className: "cat-dots" })
  for (let d = 0; d < slides_count; d++) {
    let dot = dom.create("button", {
      className: "cat-dot" + (d === 0 ? " active" : ""),
      attrs: { "data-dot-cat": c + "", "data-dot-idx": d + "" }
    })
    dom.on(dot, "click", () => { cat_positions[c].set(d) })
    dom.add_child(dots_div, dot)
  }

  // Arrows
  let prev_btn = dom.create("button", { className: "cat-arrow", text: "←" })
  dom.on(prev_btn, "click", () => {
    let cur = cat_positions[c].get()
    if (cur > 0) cat_positions[c].set(cur - 1)
  })
  let next_btn = dom.create("button", { className: "cat-arrow", text: "→" })
  dom.on(next_btn, "click", () => {
    let cur = cat_positions[c].get()
    let max_s = Math.ceil(cat.apps.length / 2) - 1
    if (cur < max_s) cat_positions[c].set(cur + 1)
  })

  let nav_bar = dom.create("div", { className: "cat-nav", children: [prev_btn, dots_div, next_btn] })

  // Slide position tracking
  cat_slide_pos.on_change((val) => {
    cat_track.style.transform = "translateX(-" + (val * 100) + "%)"
    let all_dots = dots_div.querySelectorAll(".cat-dot")
    for (const dt of all_dots) { dt.classList.remove("active") }
    if (all_dots[val]) all_dots[val].classList.add("active")
  })

  let panel = dom.create("div", {
    className: "cat-carousel",
    styles: { display: c === 0 ? "block" : "none" },
    attrs: { "data-cat-panel": c + "" },
    children: [cat_track, nav_bar]
  })
  dom.add_child(panels_wrapper, panel)
}

// Tab switching
active_cat.on_change((val) => {
  let all_tabs = dom.select_all(".cat-tab")
  for (const t of all_tabs) { t.classList.remove("active") }
  if (all_tabs[val]) all_tabs[val].classList.add("active")
  let all_panels = dom.select_all("[data-cat-panel]")
  for (const p of all_panels) { p.style.display = "none" }
  let active_panel = dom.select("[data-cat-panel='" + val + "']")
  if (active_panel) active_panel.style.display = "block"
})

let cat_section = dom.create("section", {
  id: "categories",
  children: [
    dom.create("div", { className: "section-label reveal", text: "// 38 APPLICATIONS" }),
    dom.create("h2", { className: "section-title gradient-text reveal", text: "Ecosystem by Category" }),
    dom.create("p", { className: "section-desc reveal", text: "Every application unified by Trust Layer SSO, Stripe payments, Resend email, and Twilio communications. Browse by vertical." }),
    dom.create("div", { className: "reveal", children: [tab_container, panels_wrapper] })
  ]
})
dom.mount(cat_section, "#app")



// ─── UNIFIED INFRASTRUCTURE ────────────────────────────────

dom.inject_css(`
  .infra-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.25rem;
  }
  .infra-card {
    padding: 2rem 1.5rem;
    border-radius: 1rem;
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    text-align: center;
    transition: transform 0.3s ease, border-color 0.3s ease;
    box-shadow: var(--glass-shadow);
  }
  .infra-card:hover {
    transform: translateY(-4px);
    border-color: rgba(6,182,212,0.25);
  }
  .infra-icon { font-size: 2rem; margin-bottom: 1rem; display: block; }
  .infra-card h4 { font-size: 1rem; font-weight: 700; margin-bottom: 0.4rem; }
  .infra-card p { font-size: 0.82rem; color: var(--text-secondary); line-height: 1.5; }
  .infra-card .infra-stat {
    margin-top: 1rem;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--cyan);
    padding: 0.3rem 0.75rem;
    border-radius: 0.25rem;
    background: rgba(6,182,212,0.08);
    border: 1px solid rgba(6,182,212,0.15);
    display: inline-block;
  }
  @media (max-width: 768px) {
    .infra-grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 480px) {
    .infra-grid { grid-template-columns: 1fr; }
  }
`, "dwsc-infra-styles")

let infra_section = dom.create("section", {
  id: "infrastructure",
  children: [
    dom.create("div", { className: "section-label reveal", text: "// BACKBONE" }),
    dom.create("h2", { className: "section-title gradient-text reveal", text: "Unified Infrastructure" }),
    dom.create("p", { className: "section-desc reveal", text: "Every ecosystem application shares the same authentication, payments, email, and communications backbone." }),
    dom.create("div", {
      className: "infra-grid",
      children: [
        dom.create("div", { className: "infra-card reveal", children: [
          dom.create("span", { className: "infra-icon", text: "🔐" }),
          dom.create("h4", { text: "Trust Layer SSO" }),
          dom.create("p", { text: "One identity across all 38 apps. Blockchain-verified hallmarks." }),
          dom.create("span", { className: "infra-stat", text: "38 apps connected" })
        ]}),
        dom.create("div", { className: "infra-card reveal", children: [
          dom.create("span", { className: "infra-icon", text: "📧" }),
          dom.create("h4", { text: "Resend" }),
          dom.create("p", { text: "Transactional email service. Welcome, notifications, and receipts." }),
          dom.create("span", { className: "infra-stat", text: "Email backbone" })
        ]}),
        dom.create("div", { className: "infra-card reveal", children: [
          dom.create("span", { className: "infra-icon", text: "💳" }),
          dom.create("h4", { text: "Stripe" }),
          dom.create("p", { text: "Unified payment processing with connected accounts per app." }),
          dom.create("span", { className: "infra-stat", text: "All commerce" })
        ]}),
        dom.create("div", { className: "infra-card reveal", children: [
          dom.create("span", { className: "infra-icon", text: "📱" }),
          dom.create("h4", { text: "Twilio" }),
          dom.create("p", { text: "SMS, voice, and WhatsApp messaging across the ecosystem." }),
          dom.create("span", { className: "infra-stat", text: "Communications" })
        ]})
      ]
    })
  ]
})
dom.mount(infra_section, "#app")


// ─── TECHNOLOGY STACK ──────────────────────────────────────

dom.inject_css(`
  .tech-strip {
    display: flex;
    flex-wrap: wrap;
    gap: 1.25rem;
    justify-content: center;
    margin-top: 2rem;
  }
  .tech-badge {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.65rem 1.25rem;
    border-radius: 0.75rem;
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--text-secondary);
    transition: all 0.3s ease;
  }
  .tech-badge:hover {
    border-color: rgba(6,182,212,0.3);
    color: var(--text-primary);
    transform: translateY(-2px);
  }
  .tech-badge .tech-icon { font-size: 1.2rem; }
`, "dwsc-tech-styles")

let tech_items = [
  { icon: "💎", name: "Lume" },
  { icon: "⚡", name: "Node.js" },
  { icon: "⚛️", name: "React" },
  { icon: "🔺", name: "Three.js" },
  { icon: "🚂", name: "Express" },
  { icon: "🐘", name: "PostgreSQL" },
  { icon: "🌐", name: "Neon" },
  { icon: "🚀", name: "Render" },
  { icon: "▲", name: "Vercel" },
  { icon: "📧", name: "Resend" },
  { icon: "💳", name: "Stripe" },
  { icon: "📱", name: "Twilio" }
]

let tech_strip = dom.create("div", { className: "tech-strip" })
for (const tech of tech_items) {
  dom.add_child(tech_strip, dom.create("div", {
    className: "tech-badge",
    children: [
      dom.create("span", { className: "tech-icon", text: tech.icon }),
      dom.create("span", { text: tech.name })
    ]
  }))
}

let tech_section = dom.create("section", {
  id: "tech-stack",
  children: [
    dom.create("div", { className: "section-label reveal", text: "// BUILT WITH" }),
    dom.create("h2", { className: "section-title gradient-text reveal", text: "Technology Stack" }),
    dom.create("p", { className: "section-desc reveal", text: "A modern, battle-tested stack powering every layer of the ecosystem." }),
    dom.create("div", { className: "reveal", children: [tech_strip] })
  ]
})
dom.mount(tech_section, "#app")


// ─── ROADMAP TIMELINE ──────────────────────────────────────

dom.inject_css(`
  .timeline {
    position: relative;
    max-width: 700px;
    margin: 2rem auto 0;
    padding-left: 2rem;
  }
  .timeline::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 2px;
    background: linear-gradient(to bottom, var(--cyan), var(--purple));
    opacity: 0.3;
  }
  .tl-item {
    position: relative;
    padding-left: 2rem;
    margin-bottom: 1.5rem;
  }
  .tl-item::before {
    content: '';
    position: absolute;
    left: -5px;
    top: 6px;
    width: 12px;
    height: 12px;
    border-radius: 50%;
    border: 2px solid var(--cyan);
    background: var(--void);
  }
  .tl-item.done::before {
    background: var(--cyan);
    box-shadow: 0 0 10px rgba(6,182,212,0.4);
  }
  .tl-item.current::before {
    background: var(--purple);
    border-color: var(--purple);
    box-shadow: 0 0 10px rgba(168,85,247,0.4);
    animation: pulse 2s ease infinite;
  }
  .tl-label {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--cyan);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 0.25rem;
  }
  .tl-title {
    font-size: 1rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 0.25rem;
  }
  .tl-desc {
    font-size: 0.82rem;
    color: var(--text-secondary);
    line-height: 1.5;
  }
  .tl-item.future .tl-title { color: var(--text-secondary); }
  .tl-item.future .tl-label { opacity: 0.5; }
  .tl-item.future::before {
    border-color: var(--text-dim);
    background: transparent;
  }
  @media (max-width: 480px) {
    .timeline { padding-left: 1.5rem; }
    .tl-item { padding-left: 1.5rem; }
  }
`, "dwsc-timeline-styles")

let milestones = [
  { id: "M1-M7", title: "Core Compiler + English Mode + Security", desc: "Lexer, parser, transpiler, 102 patterns, 7-layer tolerance, 3-layer security, intent blocks.", status: "done" },
  { id: "M8", title: "Voice-to-Code Pipeline", desc: "7-step transcription cleanup, Web Speech API, homophone resolution, filler stripping.", status: "done" },
  { id: "M9", title: "Interactive Playground", desc: "In-browser compilation, sandboxed execution, multi-panel IDE with live preview.", status: "done" },
  { id: "M10", title: "Visual Context Awareness + LaaS", desc: "UI Element Registry, spatial terms, Lume as a Service (LaaS) cloud deployment. DWSC.io built entirely in Lume.", status: "done" },
  { id: "M11", title: "Reverse Mode (Code-to-Language)", desc: "lume explain — convert existing JS/TS to natural language annotations.", status: "current" },
  { id: "M12", title: "Collaborative Intent", desc: "Multi-developer compilation, AST-level diffing, intent-aware conflict resolution.", status: "future" },
  { id: "M13", title: "Zero-Dependency Runtime", desc: "Native binaries from natural language via Bun compile. One file in, one executable out.", status: "future" }
]

let timeline_div = dom.create("div", { className: "timeline" })
for (const m of milestones) {
  dom.add_child(timeline_div, dom.create("div", {
    className: "tl-item " + m.status + " reveal",
    children: [
      dom.create("div", { className: "tl-label", text: m.id }),
      dom.create("div", { className: "tl-title", text: m.title }),
      dom.create("div", { className: "tl-desc", text: m.desc })
    ]
  }))
}

let roadmap_section = dom.create("section", {
  id: "roadmap",
  children: [
    dom.create("div", { className: "section-label reveal", text: "// ROADMAP" }),
    dom.create("h2", { className: "section-title gradient-text reveal", text: "Development Timeline" }),
    dom.create("p", { className: "section-desc reveal", text: "13 milestones from concept to production-ready natural language compiler." }),
    timeline_div
  ]
})
dom.mount(roadmap_section, "#app")



dom.inject_css(`
  .vert-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    margin-top: 2rem;
  }
  .vert-grid .vert-row-2 {
    display: contents;
  }
  .vert-card {
    position: relative;
    border-radius: 1rem;
    overflow: hidden;
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    box-shadow: var(--glass-shadow);
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1), border-color 0.4s ease, box-shadow 0.4s ease;
    cursor: default;
    display: flex;
    flex-direction: column;
  }
  .vert-card:hover {
    transform: translateY(-6px);
    border-color: rgba(6, 182, 212, 0.3);
    box-shadow: 0 20px 60px rgba(6, 182, 212, 0.1), var(--glass-shadow);
  }
  .vert-card .vert-img {
    width: 100%;
    height: 180px;
    object-fit: cover;
    display: block;
    filter: brightness(0.85);
    transition: filter 0.4s ease;
  }
  .vert-card:hover .vert-img {
    filter: brightness(1);
  }
  .vert-card .vert-body {
    padding: 1.5rem;
    flex: 1;
    display: flex;
    flex-direction: column;
  }
  .vert-card .vert-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-family: var(--font-mono);
    font-size: 0.65rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--cyan);
    margin-bottom: 0.75rem;
    opacity: 0.8;
  }
  .vert-card .vert-badge .dot {
    width: 5px;
    height: 5px;
    background: var(--cyan);
    border-radius: 50%;
    animation: pulse 2s ease infinite;
  }
  .vert-card h3 {
    font-size: 1.15rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
  }
  .vert-card .vert-desc {
    font-size: 0.88rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin-bottom: 1rem;
    flex: 1;
  }
  .vert-card .vert-code {
    background: var(--primary);
    border: 1px solid var(--glass-border);
    border-radius: 0.5rem;
    padding: 0.75rem 1rem;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    line-height: 1.6;
    color: var(--text-secondary);
    overflow-x: auto;
  }
  .vert-card .vert-code .kw { color: var(--purple); }
  .vert-card .vert-code .fn { color: var(--cyan); }
  .vert-card .vert-code .str { color: var(--teal); }
  .vert-card .vert-code .cm { color: var(--text-dim); }
  .vert-card.vert-wide {
    grid-column: span 2;
  }
  @media (max-width: 768px) {
    .vert-grid { grid-template-columns: 1fr; }
    .vert-card.vert-wide { grid-column: span 1; }
    .vert-card .vert-img { height: 160px; }
  }
`, "dwsc-verticals-styles")

let verticals_section = dom.create("section", {
  id: "verticals",
  children: [
    dom.create("div", { className: "section-label reveal", text: "// VERTICAL APPLICATIONS" }),
    dom.create("h2", { className: "section-title gradient-text reveal", text: "From Language to Platform" }),
    dom.create("p", { className: "section-desc reveal", text: "Five domain-specific verticals prove Lume's cognitive distance minimization generalizes across DevOps, testing, configuration, education, and accessibility — each as first-class language constructs." }),

    // Row 1: Deploy + Verify + Config
    dom.create("div", {
      className: "vert-grid reveal",
      children: [
        // Deploy Engine
        dom.create("div", { className: "vert-card", children: [
          dom.create("img", { className: "vert-img", attrs: { src: "images/deploy-engine.png", alt: "Deploy Engine", loading: "lazy" } }),
          dom.create("div", { className: "vert-body", children: [
            dom.create("div", { className: "vert-badge", children: [
              dom.create("span", { className: "dot" }),
              dom.create("span", { text: "DevOps" })
            ]}),
            dom.create("h3", { className: "gradient-text", text: "Deploy Engine" }),
            dom.create("p", { className: "vert-desc", text: "Deployment as a language keyword. No shell scripts, no CI/CD YAML — just state your intent. Self-healing pipeline with automatic rollback." }),
            dom.create("div", { className: "vert-code", html: '<span class="kw">deploy</span> <span class="fn">to</span> render <span class="fn">from</span> <span class="str">"main"</span>\n<span class="kw">deploy</span> status\n<span class="kw">deploy</span> rollback' })
          ]})
        ]}),

        // Verify Keyword
        dom.create("div", { className: "vert-card", children: [
          dom.create("img", { className: "vert-img", attrs: { src: "images/verify-keyword.png", alt: "Verify Keyword", loading: "lazy" } }),
          dom.create("div", { className: "vert-body", children: [
            dom.create("div", { className: "vert-badge", children: [
              dom.create("span", { className: "dot" }),
              dom.create("span", { text: "Testing" })
            ]}),
            dom.create("h3", { className: "gradient-text", text: "Verify Keyword" }),
            dom.create("p", { className: "vert-desc", text: "Natural language assertions at the compiler level. Zero transformation dimensions — write exactly what you'd say to a colleague." }),
            dom.create("div", { className: "vert-code", html: '<span class="kw">verify</span> response.status <span class="fn">is</span> <span class="str">200</span>\n<span class="kw">verify</span> users <span class="fn">is not</span> empty\n<span class="kw">verify</span> count <span class="fn">is greater than</span> <span class="str">5</span>' })
          ]})
        ]}),

        // Config Language
        dom.create("div", { className: "vert-card", children: [
          dom.create("img", { className: "vert-img", attrs: { src: "images/config-engine.png", alt: "Config Engine", loading: "lazy" } }),
          dom.create("div", { className: "vert-body", children: [
            dom.create("div", { className: "vert-badge", children: [
              dom.create("span", { className: "dot" }),
              dom.create("span", { text: "Configuration" })
            ]}),
            dom.create("h3", { className: "gradient-text", text: "Config Language" }),
            dom.create("p", { className: "vert-desc", text: "Type-safe configuration as code. Replace YAML, TOML, and JSON with validated config blocks and env() integration." }),
            dom.create("div", { className: "vert-code", html: '<span class="kw">config</span> database = postgres\n  <span class="fn">at</span> <span class="str">"db.example.com"</span>\n  <span class="fn">port</span> <span class="str">5432</span>' })
          ]})
        ]}),

        // Education Mode (wide)
        dom.create("div", { className: "vert-card vert-wide", children: [
          dom.create("img", { className: "vert-img", attrs: { src: "images/education-mode.png", alt: "Education Mode", loading: "lazy" } }),
          dom.create("div", { className: "vert-body", children: [
            dom.create("div", { className: "vert-badge", children: [
              dom.create("span", { className: "dot" }),
              dom.create("span", { text: "Education" })
            ]}),
            dom.create("h3", { className: "gradient-text", text: "Education Mode" }),
            dom.create("p", { className: "vert-desc", text: "Beginner-tuned Tolerance Chain that accepts natural language drawing commands. Friendly error messages guide learners instead of intimidating them with stack traces." }),
            dom.create("div", { className: "vert-code", html: '<span class="kw">make</span> a big red circle\n<span class="kw">add</span> a button that says hello\n\n<span class="cm">// Error: 🎨 Try "draw a red circle" or "make a blue square"</span>' })
          ]})
        ]}),

        // Accessibility (single)
        dom.create("div", { className: "vert-card", children: [
          dom.create("img", { className: "vert-img", attrs: { src: "images/accessibility.png", alt: "Accessibility", loading: "lazy" } }),
          dom.create("div", { className: "vert-body", children: [
            dom.create("div", { className: "vert-badge", children: [
              dom.create("span", { className: "dot" }),
              dom.create("span", { text: "Accessibility" })
            ]}),
            dom.create("h3", { className: "gradient-text", text: "Eyes-Free Pipeline" }),
            dom.create("p", { className: "vert-desc", text: "Complete eyes-free programming. Spoken errors, voice navigation, spoken deploy status, and spoken verification results. Zero screens, zero keyboards." }),
            dom.create("div", { className: "vert-code", html: '<span class="cm">// Speak → Code → Hear Results</span>\n<span class="kw">voice</span> <span class="str">"deploy status"</span>\n<span class="cm">// 🔊 "Deploy to render succeeded"</span>' })
          ]})
        ]})
      ]
    })
  ]
})
dom.mount(verticals_section, "#app")



// ─── PAPERS SECTION ────────────────────────────────────────

dom.inject_css(`
  .paper-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    margin-top: 2rem;
  }
  .paper-card {
    position: relative;
    padding: 2.5rem;
    border-radius: 1rem;
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    box-shadow: var(--glass-shadow);
    overflow: hidden;
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1), border-color 0.4s ease, box-shadow 0.4s ease;
  }
  .paper-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--cyan), var(--teal), var(--purple));
    opacity: 0.7;
  }
  .paper-card:hover {
    transform: translateY(-6px);
    border-color: rgba(6, 182, 212, 0.25);
    box-shadow: 0 16px 48px rgba(6, 182, 212, 0.08), var(--glass-shadow);
  }
  .paper-card .paper-type {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--cyan);
    margin-bottom: 1rem;
    opacity: 0.8;
  }
  .paper-card .paper-type .dot {
    width: 5px;
    height: 5px;
    background: var(--cyan);
    border-radius: 50%;
    animation: pulse 2s ease infinite;
  }
  .paper-card h3 {
    font-size: 1.25rem;
    font-weight: 700;
    line-height: 1.4;
    margin-bottom: 0.75rem;
  }
  .paper-card .paper-abstract {
    font-size: 0.9rem;
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: 1.25rem;
  }
  .paper-card .paper-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 1.25rem;
  }
  .paper-card .meta-pill {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    padding: 0.25rem 0.7rem;
    border-radius: 2rem;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    background: rgba(6, 182, 212, 0.08);
    color: var(--cyan);
    border: 1px solid rgba(6, 182, 212, 0.15);
  }
  .paper-card .paper-file {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.6rem 1rem;
    border-radius: 0.5rem;
    background: var(--primary);
    border: 1px solid var(--glass-border);
    font-family: var(--font-mono);
    font-size: 0.78rem;
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.2s, border-color 0.2s, background 0.2s;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
  }
  .paper-card .paper-file:hover {
    color: var(--cyan);
    border-color: rgba(6, 182, 212, 0.3);
    background: rgba(6, 182, 212, 0.06);
  }
  .paper-card .paper-file:active {
    background: rgba(6, 182, 212, 0.12);
  }
  .paper-card .paper-file .file-icon {
    font-size: 1rem;
  }
  .paper-card.paper-featured {
    grid-column: span 2;
    border-color: rgba(6, 182, 212, 0.2);
    background: linear-gradient(135deg, rgba(6, 182, 212, 0.04), rgba(168, 85, 247, 0.03));
  }
  .paper-card.paper-featured::before {
    height: 4px;
    opacity: 1;
  }
  .paper-card.paper-featured h3 { font-size: 1.4rem; }
  @media (max-width: 768px) {
    .paper-grid { grid-template-columns: 1fr; }
    .paper-card.paper-featured { grid-column: span 1; }
  }
`, "dwsc-papers-styles")

let papers_section = dom.create("section", {
  id: "papers",
  children: [
    dom.create("div", { className: "section-label reveal", text: "// PUBLICATIONS" }),
    dom.create("h2", { className: "section-title gradient-text reveal", text: "Academic Papers" }),
    dom.create("p", { className: "section-desc reveal", text: "Peer-reviewable research publications on Lume's theoretical foundations, security model, cognitive distance framework, and experimental methodology." }),
    dom.create("div", {
      className: "paper-grid",
      children: [
        dom.create("div", {
          className: "paper-card paper-featured reveal",
          children: [
            dom.create("div", { className: "paper-type", children: [
              dom.create("span", { className: "dot" }),
              dom.create("span", { text: "Full Paper" })
            ]}),
            dom.create("h3", { className: "gradient-text", text: "Eliminating Cognitive Distance" }),
            dom.create("p", { className: "paper-abstract", text: "An AI-Native Programming Language with Natural Language Compilation, Voice Input, and Certified Security. The complete theoretical framework, 7-layer Tolerance Chain specification, Guardian security model, and experimental evaluation plan." }),
            dom.create("div", { className: "paper-meta", children: [
              dom.create("span", { className: "meta-pill", text: "15 Sections" }),
              dom.create("span", { className: "meta-pill", text: "1,500+ Lines" }),
              dom.create("span", { className: "meta-pill", text: "86,000+ Words" }),
              dom.create("span", { className: "meta-pill", text: "CHI / PLDI Target" })
            ]}),
            dom.create("a", { className: "paper-file", attrs: { href: "papers/LUME-ACADEMIC-PAPER.md", download: "LUME-ACADEMIC-PAPER.md" }, children: [
              dom.create("span", { className: "file-icon", text: "📄" }),
              dom.create("span", { text: "⬇ Download LUME-ACADEMIC-PAPER.md" })
            ]})
          ]
        }),
        dom.create("div", {
          className: "paper-card reveal",
          children: [
            dom.create("div", { className: "paper-type", children: [
              dom.create("span", { className: "dot" }),
              dom.create("span", { text: "Technical Brief" })
            ]}),
            dom.create("h3", { className: "gradient-text", text: "Complete Technical Blueprint" }),
            dom.create("p", { className: "paper-abstract", text: "Source-of-truth blueprint for paper co-authorship. All code examples, mathematical formulas, CD scoring tables, experimental designs, and the full specification of Adaptive Voice Profiles and the TrustGen domain compiler." }),
            dom.create("div", { className: "paper-meta", children: [
              dom.create("span", { className: "meta-pill", text: "16 Subsections" }),
              dom.create("span", { className: "meta-pill", text: "1,200+ Lines" }),
              dom.create("span", { className: "meta-pill", text: "69,000+ Words" }),
              dom.create("span", { className: "meta-pill", text: "§8.16 TrustGen" })
            ]}),
            dom.create("a", { className: "paper-file", attrs: { href: "papers/LUME_ACADEMIC_BRIEF.md", download: "LUME_ACADEMIC_BRIEF.md" }, children: [
              dom.create("span", { className: "file-icon", text: "📋" }),
              dom.create("span", { text: "⬇ Download LUME_ACADEMIC_BRIEF.md" })
            ]})
          ]
        })
      ]
    })
  ]
})
dom.mount(papers_section, "#app")



// ─── BLOG SECTION RENDER ────────────────────────────────────

let blog_section = dom.create("section", {
  id: "blog",
  children: [
    dom.create("div", { className: "section-label reveal", text: "// ENGINEERING BLOG" }),
    dom.create("h2", { className: "section-title gradient-text reveal", text: "From the Lab" }),
    dom.create("p", { className: "section-desc reveal", text: "Research notes, engineering deep-dives, and behind-the-scenes from the team building the world's first intent-resolving programming language." }),
    dom.create("div", {
      className: "blog-grid",
      id: "blog-grid"
    })
  ]
})

// Populate blog cards
dom.ready(() => {
  let grid = dom.select("#blog-grid")
  for (const post of blog_posts) {
    let card = dom.create("div", {
      className: post.featured ? "blog-card featured reveal" : "blog-card reveal",
      children: [
        dom.create("div", { className: "blog-category", text: post.category }),
        dom.create("h3", { text: post.title }),
        dom.create("p", { className: "blog-excerpt", text: post.excerpt }),
        dom.create("div", {
          className: "blog-meta",
          children: [
            dom.create("span", { html: "📅 " + post.date }),
            dom.create("span", { html: "⏱ " + post.readTime }),
            dom.create("span", { html: "✦ Lume" })
          ]
        })
      ]
    })
    card.addEventListener("click", () => {
      window.location.hash = "blog/" + post.slug
    })
    grid.appendChild(card)
  }

  // ── Hash Router for Blog Posts ──
  function renderBlogPost() {
    let hash = window.location.hash.replace("#", "")
    if (!hash.startsWith("blog/")) return

    let slug = hash.replace("blog/", "")
    let post = blog_posts.find(p => p.slug === slug)
    if (!post) return

    // Hide main content, show post
    let app = dom.select("#app")
    let existing = dom.select("#blog-post-view")
    if (existing) existing.remove()

    // Scroll to top
    window.scrollTo(0, 0)

    let postView = dom.create("div", {
      id: "blog-post-view",
      className: "blog-post-view",
      children: [
        dom.create("a", {
          className: "blog-post-back",
          html: "← Back to Blog",
          onClick: () => {
            window.location.hash = "blog"
            postView.remove()
            app.style.display = ""
          }
        }),
        dom.create("div", {
          className: "blog-post-header",
          children: [
            dom.create("span", { className: "blog-category", text: post.category }),
            dom.create("h1", { className: "gradient-text", text: post.title }),
            dom.create("div", {
              className: "blog-meta",
              children: [
                dom.create("span", { text: "Jason Andrews" }),
                dom.create("span", { text: post.date }),
                dom.create("span", { text: post.readTime })
              ]
            })
          ]
        }),
        dom.create("div", {
          className: "blog-post-body",
          html: post.body
        })
      ]
    })

    app.style.display = "none"
    document.body.appendChild(postView)
  }

  window.addEventListener("hashchange", () => {
    let hash = window.location.hash.replace("#", "")
    if (hash.startsWith("blog/")) {
      renderBlogPost()
    } else if (hash === "portal") {
      // Portal handled by its own module
    } else {
      let postView = dom.select("#blog-post-view")
      if (postView) postView.remove()
      let portalView = dom.select("#portal-view")
      if (portalView) portalView.remove()
      let app = dom.select("#app")
      if (app) app.style.display = ""
    }
  })

  // Check on load
  if (window.location.hash.startsWith("#blog/")) {
    renderBlogPost()
  }
})

dom.mount(blog_section, "#app")



// ─── DEVELOPER PORTAL ──────────────────────────────────────

dom.inject_css(`
  /* ── Portal Auth Screen ── */
  .portal-auth {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    padding: 2rem;
  }
  .portal-auth-card {
    padding: 3rem;
    border-radius: 1.5rem;
    background: var(--glass-bg);
    backdrop-filter: blur(24px);
    border: 1px solid var(--glass-border);
    box-shadow: var(--glass-shadow);
    text-align: center;
    max-width: 400px;
    width: 100%;
  }
  .portal-auth-card h2 {
    font-size: 1.5rem;
    font-weight: 800;
    margin-bottom: 0.5rem;
  }
  .portal-auth-card .portal-sub {
    color: var(--text-secondary);
    font-size: 0.9rem;
    margin-bottom: 2rem;
  }
  .pin-input-row {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
    margin-bottom: 1.5rem;
  }
  .pin-digit {
    width: 52px;
    height: 60px;
    text-align: center;
    font-size: 1.5rem;
    font-weight: 700;
    font-family: var(--font-mono);
    background: var(--primary);
    border: 1px solid var(--glass-border);
    border-radius: 0.75rem;
    color: var(--cyan);
    outline: none;
    transition: border-color 0.2s;
  }
  .pin-digit:focus {
    border-color: var(--cyan);
    box-shadow: 0 0 12px rgba(6, 182, 212, 0.2);
  }
  .pin-error {
    color: #ef4444;
    font-size: 0.85rem;
    margin-top: 0.5rem;
    min-height: 1.25rem;
  }
  .portal-enter-btn {
    padding: 0.75rem 2.5rem;
    border-radius: 2rem;
    background: linear-gradient(135deg, var(--cyan), #0891b2);
    border: none;
    color: #fff;
    font-size: 0.95rem;
    font-weight: 700;
    cursor: pointer;
    transition: all 0.3s ease;
  }
  .portal-enter-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(6, 182, 212, 0.3);
  }

  /* ── Portal Dashboard ── */
  .portal-dashboard {
    max-width: 1100px;
    margin: 0 auto;
    padding: 5rem 2rem 3rem;
  }
  .portal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--glass-border);
  }
  .portal-header h1 {
    font-size: 1.8rem;
    font-weight: 800;
  }
  .portal-exit-btn {
    padding: 0.5rem 1.25rem;
    border-radius: 2rem;
    background: rgba(239, 68, 68, 0.1);
    border: 1px solid rgba(239, 68, 68, 0.3);
    color: #ef4444;
    font-size: 0.8rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
  }
  .portal-exit-btn:hover {
    background: rgba(239, 68, 68, 0.2);
  }
  .portal-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
    margin-bottom: 2rem;
  }
  .portal-card {
    padding: 1.5rem;
    border-radius: 1rem;
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    box-shadow: var(--glass-shadow);
  }
  .portal-card .portal-card-label {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--cyan);
    letter-spacing: 0.15em;
    text-transform: uppercase;
    margin-bottom: 0.5rem;
  }
  .portal-card .portal-card-value {
    font-size: 2rem;
    font-weight: 900;
  }
  .portal-card .portal-card-sub {
    font-size: 0.8rem;
    color: var(--text-secondary);
    margin-top: 0.25rem;
  }
  .portal-section {
    margin-bottom: 2rem;
  }
  .portal-section h3 {
    font-size: 1.1rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: var(--cyan);
  }
  .health-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.75rem;
  }
  .health-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1rem;
    border-radius: 0.75rem;
    background: var(--primary);
    border: 1px solid var(--glass-border);
    font-size: 0.85rem;
  }
  .health-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }
  .health-dot.up { background: #22c55e; box-shadow: 0 0 8px rgba(34, 197, 94, 0.5); }
  .health-dot.down { background: #ef4444; box-shadow: 0 0 8px rgba(239, 68, 68, 0.5); }
  .health-dot.checking { background: #eab308; animation: pulse 1s ease infinite; }
  .health-name { flex: 1; color: var(--text-secondary); }
  .health-status { font-family: var(--font-mono); font-size: 0.75rem; }
  .health-status.up { color: #22c55e; }
  .health-status.down { color: #ef4444; }
  .health-status.checking { color: #eab308; }

  .blog-mgmt-table {
    width: 100%;
    border-collapse: collapse;
  }
  .blog-mgmt-table th {
    text-align: left;
    padding: 0.6rem 1rem;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--cyan);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    border-bottom: 1px solid var(--glass-border);
  }
  .blog-mgmt-table td {
    padding: 0.75rem 1rem;
    font-size: 0.85rem;
    color: var(--text-secondary);
    border-bottom: 1px solid rgba(255,255,255,0.04);
  }
  .blog-mgmt-table tr:hover td {
    background: rgba(6, 182, 212, 0.03);
  }
  .quick-actions {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
  }
  .quick-action-btn {
    padding: 0.5rem 1.25rem;
    border-radius: 2rem;
    background: rgba(6, 182, 212, 0.06);
    border: 1px solid rgba(6, 182, 212, 0.15);
    color: var(--cyan);
    font-size: 0.8rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s;
    text-decoration: none;
  }
  .quick-action-btn:hover {
    background: rgba(6, 182, 212, 0.12);
    border-color: rgba(6, 182, 212, 0.3);
  }
  .portal-timestamp {
    text-align: center;
    padding: 1.5rem;
    font-size: 0.75rem;
    color: var(--text-dim);
    font-family: var(--font-mono);
  }
  @media (max-width: 768px) {
    .portal-grid { grid-template-columns: 1fr; }
    .health-grid { grid-template-columns: 1fr; }
    .pin-digit { width: 44px; height: 52px; font-size: 1.2rem; }
  }
`, "dwsc-portal-styles")


// ─── PORTAL ANALYTICS TRACKER ──────────────────────────────

// Track page views in localStorage
dom.ready(() => {
  let views = parseInt(localStorage.getItem("dwsc_views") || "0")
  localStorage.setItem("dwsc_views", String(views + 1))

  let sessions = parseInt(localStorage.getItem("dwsc_sessions") || "0")
  if (!sessionStorage.getItem("dwsc_session_active")) {
    sessionStorage.setItem("dwsc_session_active", "1")
    localStorage.setItem("dwsc_sessions", String(sessions + 1))
  }

  // Track referrer
  if (document.referrer) {
    let refs = JSON.parse(localStorage.getItem("dwsc_referrers") || "[]")
    refs.push({ url: document.referrer, time: Date.now() })
    if (refs.length > 100) refs = refs.slice(-100)
    localStorage.setItem("dwsc_referrers", JSON.stringify(refs))
  }

  // Track page load times
  let loadTimes = JSON.parse(localStorage.getItem("dwsc_load_times") || "[]")
  let loadTime = performance.now()
  loadTimes.push({ time: Date.now(), ms: Math.round(loadTime) })
  if (loadTimes.length > 50) loadTimes = loadTimes.slice(-50)
  localStorage.setItem("dwsc_load_times", JSON.stringify(loadTimes))
})


// ─── PORTAL ROUTE HANDLER ──────────────────────────────────

dom.ready(() => {
  let PORTAL_PIN = "0424"
  let portalAuthenticated = false

  function showPortalAuth() {
    let app = dom.select("#app")
    let existing = dom.select("#portal-view")
    if (existing) existing.remove()
    app.style.display = "none"
    window.scrollTo(0, 0)

    let errorMsg = dom.create("div", { className: "pin-error", text: "" })

    let digits = []
    let pinRow = dom.create("div", { className: "pin-input-row" })
    for (let i = 0; i < 4; i++) {
      let input = dom.create("input", {
        className: "pin-digit",
        attrs: { type: "password", maxlength: "1", inputmode: "numeric", autocomplete: "off" }
      })
      input.addEventListener("input", (e) => {
        if (e.target.value.length === 1 && i < 3) {
          digits[i + 1].focus()
        }
        errorMsg.textContent = ""
      })
      input.addEventListener("keydown", (e) => {
        if (e.key === "Backspace" && !e.target.value && i > 0) {
          digits[i - 1].focus()
        }
        if (e.key === "Enter") {
          attemptLogin()
        }
      })
      digits.push(input)
      pinRow.appendChild(input)
    }

    function attemptLogin() {
      let pin = digits.map(d => d.value).join("")
      if (pin === PORTAL_PIN) {
        portalAuthenticated = true
        showPortalDashboard()
      } else {
        errorMsg.textContent = "Invalid PIN. Access denied."
        digits.forEach(d => { d.value = ""; d.style.borderColor = "#ef4444" })
        digits[0].focus()
        setTimeout(() => digits.forEach(d => d.style.borderColor = ""), 1500)
      }
    }

    let enterBtn = dom.create("button", {
      className: "portal-enter-btn",
      text: "◈ Enter Portal",
      onClick: attemptLogin
    })

    let authView = dom.create("div", {
      id: "portal-view",
      className: "portal-auth",
      children: [
        dom.create("div", {
          className: "portal-auth-card",
          children: [
            dom.create("div", { className: "nav-logo gradient-text", text: "DWSC", styles: { fontSize: "1.3rem", marginBottom: "1rem" } }),
            dom.create("h2", { text: "Developer Portal" }),
            dom.create("p", { className: "portal-sub", text: "Enter your 4-digit developer PIN" }),
            pinRow,
            errorMsg,
            enterBtn
          ]
        })
      ]
    })

    document.body.appendChild(authView)
    setTimeout(() => digits[0].focus(), 100)
  }

  function showPortalDashboard() {
    let app = dom.select("#app")
    let existing = dom.select("#portal-view")
    if (existing) existing.remove()
    app.style.display = "none"
    window.scrollTo(0, 0)

    // Gather analytics
    let totalViews = localStorage.getItem("dwsc_views") || "0"
    let totalSessions = localStorage.getItem("dwsc_sessions") || "0"
    let referrers = JSON.parse(localStorage.getItem("dwsc_referrers") || "[]")
    let loadTimes = JSON.parse(localStorage.getItem("dwsc_load_times") || "[]")
    let avgLoad = loadTimes.length > 0 ? Math.round(loadTimes.reduce((s, t) => s + t.ms, 0) / loadTimes.length) : 0

    // Unique referrer domains
    let refDomains = {}
    referrers.forEach(r => {
      try {
        let domain = new URL(r.url).hostname
        refDomains[domain] = (refDomains[domain] || 0) + 1
      } catch {}
    })
    let topRefs = Object.entries(refDomains).sort((a, b) => b[1] - a[1]).slice(0, 5)

    // Build health check items
    let healthTargets = [
      { name: "dwtl.io", url: "https://dwtl.io" },
      { name: "TrustGen", url: "https://trustgen.tlid.io" },
      { name: "Lume Lang", url: "https://lume-lang.com" },
      { name: "DarkWave Studios", url: "https://darkwavestudios.io" },
      { name: "Trust Hub", url: "https://trusthub.tlid.io" },
      { name: "DWSC.io", url: "https://dwsc.io" }
    ]

    let healthGrid = dom.create("div", { className: "health-grid" })
    healthTargets.forEach(target => {
      let dot = dom.create("span", { className: "health-dot checking" })
      let status = dom.create("span", { className: "health-status checking", text: "checking..." })
      let item = dom.create("div", {
        className: "health-item",
        children: [
          dot,
          dom.create("span", { className: "health-name", text: target.name }),
          status
        ]
      })
      healthGrid.appendChild(item)

      // Ping with fetch
      fetch(target.url, { method: "HEAD", mode: "no-cors", cache: "no-store" })
        .then(() => {
          dot.className = "health-dot up"
          status.className = "health-status up"
          status.textContent = "● online"
        })
        .catch(() => {
          dot.className = "health-dot down"
          status.className = "health-status down"
          status.textContent = "● offline"
        })
    })

    // Blog management table
    let blogTable = dom.create("table", { className: "blog-mgmt-table" })
    let thead = dom.create("thead", { html: "<tr><th>Title</th><th>Category</th><th>Read Time</th><th>Date</th></tr>" })
    blogTable.appendChild(thead)
    let tbody = dom.create("tbody")
    blog_posts.forEach(post => {
      let row = dom.create("tr", {
        html: "<td>" + post.title + "</td><td>" + post.category + "</td><td>" + post.readTime + "</td><td>" + post.date + "</td>"
      })
      tbody.appendChild(row)
    })
    blogTable.appendChild(tbody)

    // Referrer list
    let refList = topRefs.length > 0
      ? topRefs.map(r => dom.create("div", { className: "health-item", children: [
          dom.create("span", { className: "health-name", text: r[0] }),
          dom.create("span", { className: "health-status up", text: r[1] + " visits" })
        ]}))
      : [dom.create("div", { className: "health-item", children: [
          dom.create("span", { className: "health-name", text: "No referrers tracked yet" })
        ]})]

    let dashboard = dom.create("div", {
      id: "portal-view",
      className: "portal-dashboard",
      children: [
        // Header
        dom.create("div", {
          className: "portal-header",
          children: [
            dom.create("h1", { className: "gradient-text", text: "◈ Developer Portal" }),
            dom.create("button", {
              className: "portal-exit-btn",
              text: "✕ Exit Portal",
              onClick: () => {
                window.location.hash = ""
                dashboard.remove()
                app.style.display = ""
                portalAuthenticated = false
              }
            })
          ]
        }),

        // Stats cards
        dom.create("div", {
          className: "portal-grid",
          children: [
            dom.create("div", { className: "portal-card", children: [
              dom.create("div", { className: "portal-card-label", text: "PAGE VIEWS" }),
              dom.create("div", { className: "portal-card-value gradient-text", text: totalViews }),
              dom.create("div", { className: "portal-card-sub", text: "All time (local)" })
            ]}),
            dom.create("div", { className: "portal-card", children: [
              dom.create("div", { className: "portal-card-label", text: "SESSIONS" }),
              dom.create("div", { className: "portal-card-value gradient-text", text: totalSessions }),
              dom.create("div", { className: "portal-card-sub", text: "Unique visits" })
            ]}),
            dom.create("div", { className: "portal-card", children: [
              dom.create("div", { className: "portal-card-label", text: "AVG LOAD" }),
              dom.create("div", { className: "portal-card-value gradient-text", text: avgLoad + "ms" }),
              dom.create("div", { className: "portal-card-sub", text: "Last " + loadTimes.length + " loads" })
            ]})
          ]
        }),

        // System health
        dom.create("div", {
          className: "portal-section",
          children: [
            dom.create("h3", { text: "🔒 System Health" }),
            healthGrid
          ]
        }),

        // Referrers
        dom.create("div", {
          className: "portal-section",
          children: [
            dom.create("h3", { text: "📊 Top Referrers" }),
            dom.create("div", { className: "health-grid", children: refList })
          ]
        }),

        // Blog management
        dom.create("div", {
          className: "portal-section",
          children: [
            dom.create("h3", { text: "📝 Blog Posts (" + blog_posts.length + ")" }),
            blogTable
          ]
        }),

        // Quick actions
        dom.create("div", {
          className: "portal-section",
          children: [
            dom.create("h3", { text: "⚡ Quick Actions" }),
            dom.create("div", {
              className: "quick-actions",
              children: [
                dom.create("a", { className: "quick-action-btn", text: "📄 View Papers", attrs: { href: "#papers" } }),
                dom.create("a", { className: "quick-action-btn", text: "📝 View Blog", attrs: { href: "#blog" } }),
                dom.create("a", { className: "quick-action-btn", text: "🌐 dwtl.io", attrs: { href: "https://dwtl.io", target: "_blank" } }),
                dom.create("a", { className: "quick-action-btn", text: "◈ TrustGen IDE", attrs: { href: "https://trustgen.tlid.io", target: "_blank" } }),
                dom.create("a", { className: "quick-action-btn", text: "📦 GitHub", attrs: { href: "https://github.com/cryptocreeper94-sudo/dwsc", target: "_blank" } }),
                dom.create("button", {
                  className: "quick-action-btn",
                  text: "🗑️ Reset Analytics",
                  onClick: () => {
                    localStorage.removeItem("dwsc_views")
                    localStorage.removeItem("dwsc_sessions")
                    localStorage.removeItem("dwsc_referrers")
                    localStorage.removeItem("dwsc_load_times")
                    alert("Analytics reset. Refresh to see changes.")
                  }
                })
              ]
            })
          ]
        }),

        // Timestamp
        dom.create("div", {
          className: "portal-timestamp",
          text: "DWSC Developer Portal · Built with Lume · " + new Date().toLocaleString()
        })
      ]
    })

    document.body.appendChild(dashboard)
  }

  // Route handler for #portal
  function handlePortalRoute() {
    if (window.location.hash === "#portal") {
      if (portalAuthenticated) {
        showPortalDashboard()
      } else {
        showPortalAuth()
      }
    }
  }

  window.addEventListener("hashchange", handlePortalRoute)
  handlePortalRoute()
})




// ─── ECOSYSTEM ACCOUNT HUB V2 ──────────────────────────────

dom.inject_css(`
  .hub-trigger {
    position: fixed; top: 14px; right: 16px; z-index: 9998;
    width: 38px; height: 38px; border-radius: 50%;
    border: 2px solid rgba(6,182,212,0.35);
    background: linear-gradient(135deg,rgba(8,10,18,0.9),rgba(8,10,18,0.9));
    backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
    cursor: pointer; display: flex; align-items: center; justify-content: center;
    font-size: 15px; color: rgba(255,255,255,0.85);
    box-shadow: 0 4px 20px rgba(0,0,0,0.4); padding: 0; outline: none;
    transition: all 0.3s ease;
  }
  .hub-trigger:hover { border-color: var(--cyan); box-shadow: 0 0 15px rgba(6,182,212,0.4); }
  .hub-backdrop {
    position: fixed; inset: 0; z-index: 9998;
    background: rgba(0,0,0,0.5); backdrop-filter: blur(6px); -webkit-backdrop-filter: blur(6px);
    opacity: 0; pointer-events: none; transition: opacity 0.3s ease;
  }
  .hub-backdrop.open { opacity: 1; pointer-events: auto; }
  .hub-panel {
    position: fixed; top: 0; right: 0; z-index: 9999;
    width: 360px; max-width: 100vw; height: 100dvh;
    background: linear-gradient(180deg,rgba(8,10,18,0.98),rgba(4,6,12,0.99));
    border-left: 1px solid rgba(6,182,212,0.1);
    backdrop-filter: blur(60px); -webkit-backdrop-filter: blur(60px);
    overflow-y: auto; display: flex; flex-direction: column;
    transform: translateX(100%); transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .hub-panel.open { transform: translateX(0); }
  @media (max-width: 640px) { .hub-panel { width: 100vw; border-left: none; } }
  .hub-hdr {
    padding: 20px 20px 16px; border-bottom: 1px solid rgba(255,255,255,0.04);
    display: flex; align-items: center; gap: 14px;
  }
  .hub-av {
    width: 48px; height: 48px; border-radius: 50%;
    border: 2px solid transparent; background: linear-gradient(135deg,rgba(8,10,18,0.9),rgba(8,10,18,0.9));
    background-clip: padding-box; display: flex; align-items: center; justify-content: center;
    font-size: 20px; flex-shrink: 0; box-shadow: inset 0 0 0 2px rgba(6,182,212,0.2);
  }
  .hub-info { flex: 1; min-width: 0; }
  .hub-nm { font-size: 15px; font-weight: 700; color: rgba(255,255,255,0.92); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .hub-em { font-size: 11px; color: rgba(6,182,212,0.5); font-family: 'JetBrains Mono', monospace; margin-top: 2px; }
  .hub-close {
    width: 30px; height: 30px; border-radius: 10px; border: 1px solid rgba(255,255,255,0.04);
    background: rgba(255,255,255,0.02); color: rgba(255,255,255,0.25);
    cursor: pointer; display: flex; align-items: center; justify-content: center;
    font-size: 12px; transition: all 0.2s;
  }
  .hub-close:hover { background: rgba(255,255,255,0.1); color: #fff; }
  .hub-bonus { margin: 16px; padding: 16px; border-radius: 16px; border: 1px solid rgba(6,182,212,0.12); position: relative; overflow: hidden; }
  .hub-sec { padding: 14px 20px; border-bottom: 1px solid rgba(255,255,255,0.02); }
  .hub-lbl { font-size: 9px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.12em; color: rgba(255,255,255,0.18); margin-bottom: 8px; }
  .hub-row {
    display: flex; align-items: center; gap: 12px; padding: 10px 12px; border-radius: 12px;
    cursor: pointer; text-decoration: none; color: rgba(255,255,255,0.65); transition: background 0.2s;
  }
  .hub-row:hover { background: rgba(255,255,255,0.04); color: #fff; }
  .hub-rIcon { width: 32px; height: 32px; border-radius: 10px; background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.04); display: flex; align-items: center; justify-content: center; font-size: 14px; }
  .hub-rCont { flex: 1; }
  .hub-rTitle { font-size: 13px; font-weight: 600; }
  .hub-rSub { font-size: 10px; color: rgba(255,255,255,0.25); margin-top: 1px; }
  .hub-badge { padding: 2px 8px; border-radius: 6px; font-size: 10px; font-weight: 800; font-family: 'JetBrains Mono', monospace; }
  .hub-badgeC { background: rgba(6,182,212,0.08); border: 1px solid rgba(6,182,212,0.15); color: #67e8f9; }
  .hub-badgeG { background: rgba(16,185,129,0.08); border: 1px solid rgba(16,185,129,0.15); color: #6ee7b7; }
  .hub-grid { display: flex; flex-wrap: wrap; gap: 6px; padding: 0 12px; }
  .hub-appBadge { display: inline-flex; align-items: center; gap: 5px; padding: 5px 10px; border-radius: 8px; background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.04); text-decoration: none; color: rgba(255,255,255,0.5); font-size: 11px; font-weight: 600; transition: all 0.2s; }
  .hub-appBadge:hover { background: rgba(255,255,255,0.06); color: #fff; }
  .hub-conn { padding: 32px 24px; display: flex; flex-direction: column; align-items: center; gap: 16px; text-align: center; justify-content: center; flex: 1; }
  .hub-connBtn { display: inline-flex; align-items: center; gap: 8px; padding: 12px 32px; border-radius: 999px; background: linear-gradient(135deg, #06b6d4, #8b5cf6); color: #fff; font-size: 14px; font-weight: 700; text-decoration: none; cursor: pointer; transition: transform 0.2s, box-shadow 0.2s; border: none; }
  .hub-connBtn:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(6,182,212,0.4); }
  .hub-ft { margin-top: auto; padding: 14px 20px; border-top: 1px solid rgba(255,255,255,0.02); font-size: 10px; color: rgba(255,255,255,0.12); text-align: center; }
  .hub-ftLink { color: rgba(6,182,212,0.35); text-decoration: none; }
  .hub-ftLink:hover { color: var(--cyan); }
`, "hub-css")

// --- App Configuration & User Data ---
const SSO_URL = "https://dwtl.io"
const PRE_URL = "https://dwtl.io/presale"

const hub_apps = [{n:'Trust Hub',u:'https://trusthub.tlid.io',i:'🛡️'},{n:'TrustGen 3D',u:'https://trustgen.tlid.io',i:'🎨'},{n:'TrustVault',u:'https://trustvault.tlid.io',i:'🔐'},{n:'Chronicles',u:'https://yourlegacy.io',i:'📜'},{n:'ORBIT',u:'https://orbitstaffing.io',i:'🌐'},{n:'Lume',u:'https://lume-lang.org',i:'💡'},{n:'Bomber 3D',u:'https://bomber.tlid.io',i:'⛳'},{n:'THE VOID',u:'https://intothevoid.app',i:'🕳️'},{n:'Lot Ops',u:'https://lotopspro.io',i:'🚗'},{n:'SignalCast',u:'https://signalcast.tlid.io',i:'📡'},{n:'Studio',u:'https://studio.tlid.io',i:'🎛️'}]

let hub_user = null
let user_keys = ['tl_user','trustlayer_user','user','auth_user','dwtl_user','eco_user','vanops_user','orbit_user','trustgen_user','chronicles_user','bomber_user']

for (let key of user_keys) {
  try {
    let raw = localStorage.getItem(key)
    if (raw) {
      let parsed = JSON.parse(raw)
      if (parsed && (parsed.name || parsed.email || parsed.username || parsed.displayName)) {
        let uname = parsed.displayName || parsed.name || parsed.username || parsed.email.split('@')[0]
        hub_user = { name: uname, email: parsed.email, avatar: parsed.avatar || parsed.avatarUrl || parsed.profilePic }
        break
      }
    }
  } catch (e) {
    // ignore error
  }
}

// --- DOM Elements ---
let hub_open = state.reactive(false)
let hub_backdrop = dom.create("div", { className: "hub-backdrop" })
let hub_panel = dom.create("div", { className: "hub-panel" })

// Create trigger button
let trigger_initial = "👤"
if (hub_user) {
  if (hub_user.avatar) {
    trigger_initial = "<img src='" + hub_user.avatar + "' style='width:100%;height:100%;border-radius:50%;object-fit:cover'/>"
  } else {
    trigger_initial = "<span style='font-size:11px;font-weight:800;color:#67e8f9'>" + text.upper(hub_user.name.split(' ')[0].substring(0,2)) + "</span>"
  }
}

let hub_trigger = dom.create("button", {
  className: "hub-trigger",
  html: trigger_initial,
  onClick: () => {
    hub_open.set(!hub_open.get())
  }
})
dom.mount(hub_trigger)

// Append backdrop and panel to body
dom.mount(hub_backdrop)
dom.mount(hub_panel)

// Render Panel Content based on User State
let render_hub_panel = () => {
  dom.clear(hub_panel)
  
  let hdr_av = "👤"
  if (hub_user) {
    if (hub_user.avatar) {
      hdr_av = "<img src='" + hub_user.avatar + "' style='width:100%;height:100%;border-radius:50%;object-fit:cover'/>"
    } else {
      hdr_av = "<span style='font-weight:800;font-size:14px;color:#67e8f9'>" + text.upper(hub_user.name.split(' ')[0].substring(0,2)) + "</span>"
    }
  }
  
  let hdr_nm = hub_user ? hub_user.name : "Trust Layer"
  let hdr_em = hub_user ? (hub_user.email || "Trust Layer ID") : "Connect your account"
  
  let hdr = dom.create("div", { className: "hub-hdr", html: "<div class='hub-av'>" + hdr_av + "</div><div class='hub-info'><div class='hub-nm'>" + hdr_nm + "</div><div class='hub-em'>" + hdr_em + "</div></div>" })
  
  let close_btn = dom.create("button", { className: "hub-close", html: "✕", onClick: () => hub_open.set(false) })
  dom.add_child(hdr, close_btn)
  dom.add_child(hub_panel, hdr)

  // Bonus Section
  let bonus = dom.create("div", { className: "hub-bonus", styles: { background: "linear-gradient(135deg, rgba(6,182,212,0.06), rgba(0,0,0,0.1))" }, html: "<div style='display:flex;align-items:center;gap:6px;font-size:9px;font-weight:800;text-transform:uppercase;letter-spacing:0.14em;margin-bottom:10px;color:#06b6d4'><span style='width:6px;height:6px;border-radius:50%;background:#06b6d4'></span>🔥 This Week's Bonus</div><div style='font-size:16px;font-weight:800;color:rgba(255,255,255,0.92);margin-bottom:4px;'>💡 Language Pioneers</div><div style='font-size:12px;color:rgba(255,255,255,0.45);margin-bottom:12px;line-height:1.4'>Invite devs to Lume</div><div style='display:flex;align-items:center;gap:8px;margin-bottom:14px;flex-wrap:wrap'><span class='hub-bonusSig'>⚡ 500 SIG</span><span class='hub-bonusMult'>2×</span><span class='hub-bonusPerk'>+ v1.0 early access</span></div><a href='https://lume-lang.org?ref=eco&bonus=lume' target='_blank' style='display:flex;align-items:center;justify-content:center;gap:6px;width:100%;padding:10px 0;border-radius:10px;color:#fff;font-size:13px;font-weight:700;text-decoration:none;background:linear-gradient(135deg,#06b6d4,#8b5cf6)'>🚀 Refer & Earn</a>" })
  dom.add_child(hub_panel, bonus)

  if (hub_user) {
    dom.add_child(hub_panel, dom.create("div", { className: "hub-sec", html: "<div class='hub-lbl'>Signal Wallet</div><a class='hub-row' href='" + PRE_URL + "' target='_blank'><div class='hub-rIcon'>⚡</div><div class='hub-rCont'><div class='hub-rTitle'>Signal (SIG)</div><div class='hub-rSub'>Signal Charging · $0.001</div></div><span class='hub-badge hub-badgeC'>LIVE</span></a><a class='hub-row' href='" + SSO_URL + "/wallet' target='_blank'><div class='hub-rIcon'>💎</div><div class='hub-rCont'><div class='hub-rTitle'>Manage Wallet</div><div class='hub-rSub'>Balance, transactions</div></div></a>" }))
    dom.add_child(hub_panel, dom.create("div", { className: "hub-sec", html: "<div class='hub-lbl'>Trust & Identity</div><a class='hub-row' href='" + SSO_URL + "/hallmark' target='_blank'><div class='hub-rIcon'>🏛️</div><div class='hub-rCont'><div class='hub-rTitle'>DW-STAMP Hallmark</div><div class='hub-rSub'>Trust verification & tier</div></div><span class='hub-badge hub-badgeG'>✓</span></a><a class='hub-row' href='" + SSO_URL + "/profile' target='_blank'><div class='hub-rIcon'>🆔</div><div class='hub-rCont'><div class='hub-rTitle'>Trust Layer ID</div><div class='hub-rSub'>Manage your TLID</div></div></a>" }))
    
    let apps_html = "<div class='hub-lbl'>Ecosystem Apps</div><div class='hub-grid'>"
    for (let app of hub_apps) {
      apps_html = apps_html + "<a class='hub-appBadge' href='" + app.u + "' target='_blank'><span style='font-size:12px'>" + app.i + "</span>" + app.n + "</a>"
    }
    apps_html = apps_html + "</div>"
    dom.add_child(hub_panel, dom.create("div", { className: "hub-sec", html: apps_html }))
  } else {
    dom.add_child(hub_panel, dom.create("div", { className: "hub-conn", html: "<div style='font-size:44px;opacity:0.5'>🛡️</div><div style='font-size:17px;font-weight:800;color:rgba(255,255,255,0.88)'>Connect to Trust Layer</div><div style='font-size:12px;color:rgba(255,255,255,0.35);line-height:1.55;max-width:260px'>Sign in with your Trust Layer ID to access your wallet, hallmark, rewards, and ecosystem apps.</div><a class='hub-connBtn' href='" + SSO_URL + "/login' target='_blank'>🔗 Connect Account</a>" }))
    let apps_html = "<div class='hub-lbl'>Explore the Ecosystem</div><div class='hub-grid'>"
    for (let app of hub_apps) {
      apps_html = apps_html + "<a class='hub-appBadge' href='" + app.u + "' target='_blank'><span style='font-size:12px'>" + app.i + "</span>" + app.n + "</a>"
    }
    apps_html = apps_html + "</div>"
    dom.add_child(hub_panel, dom.create("div", { className: "hub-sec", html: apps_html }))
  }
  
  dom.add_child(hub_panel, dom.create("div", { className: "hub-ft", html: "<a class='hub-ftLink' href='" + SSO_URL + "' target='_blank'>Trust Layer</a> · Ecosystem Account Hub" }))
}

render_hub_panel()

// State bindings
hub_open.on_change((isOpen) => {
  if (isOpen) {
    dom.add_class(hub_backdrop, "open")
    dom.add_class(hub_panel, "open")
    document.body.style.overflow = "hidden"
  } else {
    dom.remove_class(hub_backdrop, "open")
    dom.remove_class(hub_panel, "open")
    document.body.style.overflow = ""
  }
})

dom.on(hub_backdrop, "click", () => hub_open.set(false))
dom.on(document, "keydown", (e) => {
  if (e.key === "Escape") {
    hub_open.set(false)
  }
})



// ─── FOOTER ────────────────────────────────────────────────

let footer = dom.create("footer", {
  className: "footer",
  children: [
    dom.create("div", {
      className: "footer-grid",
      children: [
        dom.create("div", {
          children: [
            dom.create("div", {
              className: "nav-logo gradient-text",
              text: "DWSC",
              styles: { fontSize: "1.3rem", marginBottom: "1rem", cursor: "default", userSelect: "none" },
              onClick: (() => {
                let clicks = 0
                let timer = null
                return () => {
                  clicks++
                  if (clicks === 3) {
                    clicks = 0
                    clearTimeout(timer)
                    window.location.hash = "portal"
                  } else {
                    clearTimeout(timer)
                    timer = setTimeout(() => { clicks = 0 }, 800)
                  }
                }
              })()
            }),
            dom.create("p", {
              className: "footer-brand",
              text: "DarkWave Systems Collective — Research & Engineering Division of DarkWave Studios LLC. Building the future of human-computer interaction through the Trust Layer ecosystem."
            })
          ]
        }),
        dom.create("div", {
          children: [
            dom.create("h4", { text: "Language" }),
            dom.create("a", { text: "Lume Compiler", attrs: { href: "https://lume-lang.com" } }),
            dom.create("a", { text: "Playground", attrs: { href: "https://lume-lang.com/playground" } }),
            dom.create("a", { text: "Documentation", attrs: { href: "https://lume-lang.org" } }),
            dom.create("a", { text: "GitHub", attrs: { href: "https://github.com/cryptocreeper94-sudo/lume" } })
          ]
        }),
        dom.create("div", {
          children: [
            dom.create("h4", { text: "Ecosystem" }),
            dom.create("a", { text: "Trust Layer Hub", attrs: { href: "https://dwtl.io" } }),
            dom.create("a", { text: "TrustGen 3D + Lume IDE", attrs: { href: "https://trustgen.tlid.io" } }),
            dom.create("a", { text: "DarkWave Studios", attrs: { href: "https://darkwavestudios.io" } }),
            dom.create("a", { text: "Trust Hub", attrs: { href: "https://trusthub.tlid.io" } })
          ]
        }),
        dom.create("div", {
          children: [
            dom.create("h4", { text: "Legal" }),
            dom.create("a", { text: "Privacy Policy", attrs: { href: "#" } }),
            dom.create("a", { text: "Terms of Service", attrs: { href: "#" } }),
            dom.create("a", { text: "Contact", attrs: { href: "mailto:team@dwsc.io" } })
          ]
        })
      ]
    }),
    dom.create("div", {
      className: "footer-bottom",
      html: "© 2026 DarkWave Studios LLC. All rights reserved. <br/>Powered by Lume · Trust Layer · DWSC · <a href='https://trustgen.tlid.io' style='color:var(--cyan);text-decoration:none'>Try Lume IDE →</a>"
    })
  ]
})
dom.mount(footer, "#app")


// ─── SKELETON REVEAL ───────────────────────────────────────

dom.ready(() => {
  let reveals = dom.select_all(".reveal")
  let observer = new IntersectionObserver((entries) => {
    for (const entry of entries) {
      if (entry.isIntersecting) {
        dom.add_class(entry.target, "visible")
        observer.unobserve(entry.target)
      }
    }
  }, { threshold: 0.1 })
  
  for (const el of reveals) {
    observer.observe(el)
  }
})

console.log("✦ DWSC.io rendered — Built with Lume")




// ═══ Lume Health Beacon ═══
// Signals that the IIFE executed without fatal errors
// Detectable by lume-heal.js monitor, external health checks, and browser console
window.__LUME_HEALTH__ = {
  status: 'ok',
  rendered: Date.now(),
  version: '0.8.0',
  bundle: 'dwsc',
  stages: { syntax: true, structure: true, execution: true }
};

})();
